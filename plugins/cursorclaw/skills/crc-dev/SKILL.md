---
name: crc-dev
description: "Alias of cxc-dev / dev for Cursorclaw (crc) naming. Load when the user says crc/cursorclaw; prefer the canonical `dev` skill body via references."
---

# crc-dev

This is a **naming alias** for Cursorclaw's `crc` CLI era.

- Canonical skill folder: `dev/`
- Legacy Codex skill name: `cxc-dev`

When this skill is invoked, follow `dev/SKILL.md` in this same plugin package (read that file and obey it). Do not invent a parallel process.
