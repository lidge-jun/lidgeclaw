---
description: cursorclaw core — full Cursor-native port of codexclaw discipline (hooks + skills + PABCD)
alwaysApply: true
---

# cursorclaw

This environment runs **cursorclaw**, the Cursor port of codexclaw.

## What is armed

- **Hooks** (plugin + this repo `.cursor/hooks.json`): sessionStart, beforeSubmitPrompt, preToolUse, postToolUse, preCompact, stop, subagentStop — full Codex-era component fan-out via `scripts/cursor-bridge.mjs`.
- **Skills**: every folder under `plugins/cursorclaw/skills/` (and dogfood symlinks in `.cursor/skills/`). Skill `name` matches the folder name.
- **CLI**: `crc` / `cursorclaw` (`node bin/cursorclaw.mjs`).
- **State**: `.cursorclaw/` (legacy `.codexclaw/` is migrated automatically).

## Discipline

- Classify work C0–C5 via the `dev` skill before deep process.
- Full PABCD for C4; conditional for C3; never force on C0/C1.
- Prefer evidence over intent.

## Naming

Upstream Codex used `$crc-*` skill ids and `cxc` CLI. On Cursor those are `skills/<folder>` and `crc`.
