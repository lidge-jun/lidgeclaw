---
name: orchestrate
description: Run crc orchestrate / PABCD phase control
---

# orchestrate

Use the cursorclaw CLI from the repo checkout:

```bash
node bin/cursorclaw.mjs orchestrate --help
```

Follow the matching skill under `skills/` (folder name = skill name). Prefer evidence over intent. State lives in `.cursorclaw/`.
