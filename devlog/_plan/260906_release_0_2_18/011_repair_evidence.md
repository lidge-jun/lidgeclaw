# Repair evidence

PR70 merged normally into dev as55d575ca2e989cf44c1b5ebebeed9c2cd5a794ad.
Its exact11 checks were reread successful and ancestry proved after fetch.
Local integration with PR69 retained both ownership rows; duplicate native SOT2.2
became2.3. No other checkout was reset or changed.

Independent Avicenna review approved the bounded plan after the child-isolation,
hook-owner, explicit-reset and numbering refinements. Main inspected the native
PostCompact output schema directly; it exposes universal fields only. The peer's
source reasoning used the existing repository recovery path, not live model delivery.

RED at2bde147 on macmini Node24.20.0: the7 new compact-affordance tests fail
against old output-only behavior. The first directly exposes unsupported context;
others expose missing queue/consumer/wiring. No existing assertion was weakened.

GREEN on e5a399a plus its freshly built generated files:80 tests pass,0 fail,
0 cancelled,0 skipped;822.019167ms. Selection: compact-affordance, map-affordance,
cxc-ops, goal-gate and dist-freshness. Build completed156 outputs with valid layout.
Exactly3 generated cxc-ops files changed (cli/map-affordance/reset); copied back
from the remote build for commit. The TypeScript stripping experimental warning
is retained. These checks were not run on the interactive local host.

Contract: empty PostCompact stdout; next root UserPromptSubmit gets the context
once; children cannot enqueue or unlink the parent's marker; malformed, other
session/workspace and symlinked state paths remain quiet; explicit --state clears
recovery. No PABCD session/goal/ledger files are created by this recovery mechanism.
One additional UserPromptSubmit command lives in the existing compact-affordance
JSON. Registered hook-file count is unchanged; actual trust entries are measured
at install, not assumed unchanged. Immediate same-turn/Stop recovery is not claimed.

Historical PR69 stdout-only observations remain in its archived unit; they are
superseded as delivery evidence by this supported-event lifecycle correction.
Actual model obedience or user async replies are not certified by these fixtures.

Independent source/dist repair reread at9387d78d returned no remaining blockers.
The first remote checkout of that commit was refused because its owned generated
outputs were uncommitted. Their bytes were proved identical to the target, then
checkpointed in the isolated remote clone before a normal checkout; no reset or
user work deletion. The refused checkout did not run the full suite.

Full suite then ran on clean9387d78d:2579 total,2578 pass,0 fail,0 cancelled,
1 existing optional repo-map skip;73816.479542ms. Gate passed. The existing
inventory generator updated exactly the three README test badges to measured2579.
No dependency or version changed in this repair. Original69 permission branches
remain byte-identical. Updated69 is a fast-forward, never a force push.
