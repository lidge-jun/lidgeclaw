---
created: 2026-08-15
status: evidence
workPhase: wp9
tags: [codexclaw, release, closeout]
---

# 086 — v0.2.2 릴리스 기록 및 유닛 종결

## 결과

`codexclaw v0.2.2  Latest  v0.2.2  2026-08-15T18:56:55Z`

- 릴리스 실행: `31902484632` conclusion=success
- 태그: `v0.2.2` → `541ab39c8ea51f5fb12265ebc2108f5cda873757`
- `dev`: `e70c8456..541ab39c`, `main`: `a7398d90..541ab39c`
- leniency 플래그 없이 stable

이번에는 **한 번에 통과했다.** 081이 기록한 두 번의 거부에서 배운 것을 적용했다:
버전 범프 시 테스트 배지를 실측값(1708)으로 함께 갱신했고, main 승격이
재트리거하는 CI가 끝나기를 기다린 뒤 릴리스를 실행했다.

## 유닛 전체 결산

| work-phase | 규칙 | 릴리스 |
|------------|------|--------|
| wp2 (030) | `CYCLE-COMPLETION-01` | v0.2.1 |
| wp3 (040) | `TRIGGER-AUTHORITY-01` | v0.2.1 |
| wp4 (050) | `SOURCE-DELTA-01` | v0.2.1 |
| wp6 (060) | `REVIEW-BINDING-01` | v0.2.2 |
| wp7 (070) | C>D exitCode 필수 | v0.2.2 |
| wp8 (075) | `CHECK-BINDING-01` | v0.2.2 |

네 gated edge가 전부 덮였다:

- `P>A` — 기존 `plan-gate` (번호 문서 요구)
- `A>B` — 060 (리뷰어 실물 종료 요구)
- `B>C` — 050 (소스 델타 요구)
- `C>D` — 070 + 075 (exitCode + 실행 결속 receipt)

그리고 사이클 자체를 닫는 `D`는 030이, 사이클 밖 위상 이동은 040이 막는다.

## 게이트가 저자를 막은 횟수: 다섯

051, 062에 이어 이 사이클에서도 두 번 더 막혔다.

1. attest 게이트가 FAIL 판정의 near-pass 재해석을 거부
2. `SOURCE-DELTA-01`이 wp4 사이클을 거부 (051)
3. `SOURCE-DELTA-01`이 wp5 릴리스 사이클을 거부 (081)
4. `REVIEW-BINDING-01`이 wp7/wp9의 A>B를 거부 (062)
5. `SOURCE-DELTA-01`이 이 wp9 사이클을 거부 (지금)

한 번도 우회하지 않았다. 우회 플래그를 만들지 않았고, 라운드를 손으로
`approved`로 편집하지 않았다. 매번 작업을 위상 안으로 가져오거나
(이 문서처럼) 그 사실을 기록했다.

## 다음 세션의 몫

060과 075는 **격리 프로브에서만** 실사용 검증됐다. 이 세션의 설치본은
v0.2.1(21 hooks)이라 observer 훅이 없었다(062). v0.2.2가 설치된 다음 세션이
첫 실사용 검증이 된다.

남은 것도 정직하게 적는다:

- **위조는 막지 못한다.** 리뷰어가 dummy PASS를 뱉어도, receipt를 손으로 써도
  통과한다. 두 게이트가 거부하는 것은 부재·경화·재사용이지 조작이 아니다.
- **사이클 개수는 여전히 로드맵 락의 판단이다.** 023이 세운 경계 그대로다.
  런타임이 만들 수 있는 것은 선언한 조건을 지키게 하는 것까지다.
