# WP3 C repair: prompt-time footer versus a later real transition

Status: native RED, fixed-source GREEN and old-footer-only causal toggle observed;
bounded design and P GREEN independently approved by Sartre. Toggle review is
pending. This is the existing
WP3 C repair loop, not the050 delivery experiment or a new orchestration mechanism.

## Evidence and competing explanations

At a933ba0, remote runs/wp3-candidate-n20-answer-002 first turn completes a real
CLI IDLE→I edge, pending question, semantic answer, PostToolUse/R7 and Q/A capture,
then prints IDLE. Its responses.jsonl:126 records successful I entry,185–189 the
callback,261/267 the final/completed result. The same-current-session ledger
records I. Original N20-P at runs/wp3-candidate-n20-p-001 independently does
public status(IDLE), successful CLI P, then prints IDLE (stdout.jsonl:8,12,13).

H1: prompt-time footer remains a binding literal after a real transition.
Falsifier: replacing only snapshot precedence still yields the same stale output
on the original positive prompts; restoring old wording fails to restore it.
H2: transition failed or hit the wrong session/cwd. Falsifier observed: successful
CLI output, matching latest SessionStart identity, source pin, state and ledger
agree on I/P. Reject H2 for these packets.
H3: omission of public status creates the bad report. I omitted public status,
but P inspected it and still misreported. Reject H3 as shared cause; preserve I's
methodological omission separately rather than adding unconditional calls.

Current supported mechanism: withFooter snapshots current state at prompt delivery;
phaseFooter tells the model to print that exact literal at response end. R6 now
keeps natural hints inactive, so later authorized CLI entry legitimately changes
state after the instruction. Native RED exists twice; a causal toggle is still
required before declaring the explanation verified.

## Bounded afterimage and independent review disposition

MODIFY components/pabcd-state/src/hook.ts phaseFooter return only: label the
embedded value as a prompt-time persisted snapshot; latest verified persisted
phase/label of this current SessionStart/cwd wins after a later authorized
successful transition. Failed attempts, hints and narration never change it.
No extra calls, transitions or gate bypass are authorized. D closes to IDLE unless
a subsequent authorized successful entry supersedes it. Keep signature, label map,
callers, state writes, dedup, guards, parser, attestation and receipt logic unchanged.

MODIFY skills/pabcd/references/phase-control.md: same reporting contract at its
existing status paragraph, not a new entrypoint requirement or mandatory tool call.
REGENERATE components/pabcd-state/dist/hook.js only through remote build.
MODIFY test/hook-e2e.test.mjs: registered SessionStart/advisory followed by real
agent CLI P/I, public status/state/ledger consistency; denied P→A preserves P.
Existing independent phase expectations stay unchanged. These deterministic tests
prove the state/transport seam, not model adherence to the instruction.

Sartre's read-only audit confirmed both native REDs and the direct footer tests;
VERDICT: PASS, no blocking issues for this bounded remedy. Main accepts the
afterimage and limitation. A separate new observer, automatic transition, global
status poll or additional hook would not repair the instruction's precedence.
Deleting the footer would discard the existing user-facing reporting contract;
reuse its current owner instead. File-size exceptions remain the already bounded
existing hook/test owners; no unrelated module split.

## Required verification

Use macmini only. Run existing hook/continuation/CLI/e2e/build tests and new seam
test against an isolated candidate. Record command, current source/diff and exits;
rebuild copied generated bytes and preserve no-source-drift proof. New native
original N20-P and N20-I/N21 should finally report P/I, with allowed actions only.
N19-P remains IDLE without FSM mutation. Original failures remain in the population.
An old-footer-only native toggle must restore the defect or the causal claim stays
unverified. No wording-only assertion or unrelated green suite proves native repair.

## Other C observations, not conflated with this repair

Fixed candidate fb7c39de063ad23702d8eb172dc44942eb84853b: original N20-P002
prints P/PLAN after successful status/entry, no additional polling or forbidden
action; independently reviewed PASS. Original unchanged N20-I/N21 003 prints
I/INTERVIEW after real entry/callback/R7, with no additional forbidden work;
Pauli independently confirmed footer repair, owner delivery and clean teardown.
003 still omits the separately required pre-I status, so its overall N20
admission remains FAIL; this is not silently waived by the footer GREEN.

Private semantic toggle source18b762931138f2fe460f5128fdd8a0389917bb86 is based on
fb7c39d and restores only phaseFooter plus its remotely generated dist output.
All other source/reference bytes stay fixed. Original N20-P003 again enters P
successfully but prints IDLE. Each fixed/toggle P run uses5 requests. This native
off/on observation supports the stale higher-priority snapshot mechanism, not a
universal model success rate or whole-WP3 acceptance. Originals are retained.

Remote wp3-source footer build succeeded. Existing source hook/continuation/CLI
and compiled e2e selection, including the new public seam case:277 pass,0 fail,
0 skip/0 todo (wp3-footer-check.log, duration9192ms). Only remote-generated
dist/hook.js was copied back; its local diff is the same11 added/1 removed lines
as src/hook.ts. This proves unchanged state contracts, not native footer GREEN.

N3 matched original Korean read-only probe independently reviewed by Bohr:
baseline FAIL (forbidden real test, forbidden child, incomplete owner-read repair);
candidate PASS for source-grounded read-only review, complete owner recovery and
no prohibited actions. Both source/config/payload/fixture identities checked.
Baseline's16 usage rows include499/child interruption and cannot be called parent
usage or a complete schema1 family. Candidate7 requests are not a global speedup.

N18-mismatch-001 supplemental actual paused goal creation used the approved user
API, then the exact read-only N18 prompt. Parent completed in27038ms and reported
the unrelated package-script inventory goal, paused status, IDLE and unavailable
agent resume. After-control confirms unchanged objective/createdAt/status/usage.
It ended operator-term, code0, doctor PASS, stable payload/config/launchers;
wrapper32949/server33359 absent on ps. This is an observed mismatch result,
pending independent full raw/accounting review, not the genuine blocked-goal case.

N20-answer-001 ended on stdin EOF before model/RPC work; failed transport setup.
002 kept the SSH input open via a local PTY while remote app-server stdio remained
pipes (no remote tty allocated). Native originals are the remote byte logs, never
local terminal echo. It ended after both requested turns completed, operator-term,
code0, same config/payload/launchers and doctor PASS; owned wrapper22493/server22895
were absent on ps. All three child turns completed before main's second turn.

N22's three actual Minds chose Astra/low, not required high. Its full-family
configuration condition fails independently of the observed authorized dispatch;
do not erase it as the known OCX response-tier echo. A future exact-high attempt
needs a disclosed matched fixture/control amendment, not rewritten historical data.
Both historical schema2 family packets independently passed Pauli's raw/hash/
inventory/once-only accounting review. Original no-delegation behavior remains
FAIL; no per-thread allocation, scheduler confirmation or API/V2 certification.
