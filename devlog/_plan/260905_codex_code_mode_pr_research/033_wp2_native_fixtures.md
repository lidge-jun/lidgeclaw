# WP2 native fixtures and retained observations

Status: candidate evidence, not an integrated HOTL or performance acceptance.
Controller: WP1 recorder/analyzer; native macmini Codex0.146.0 with Astra/high and
priority wire. Fresh independent homes, real marketplace installs and normal
operator-authorized hook trust per run. No probe-specific skill-loader runtime.

## Matched tasks

The operator preparation script is preserved under the session's private
evidence/operator directory. Prompts differ only in actual installed path between
variants. Config/catalog, fixture and explicit restrictions otherwise match.

| Fixture | Actual requested work | Required outcome |
| --- | --- | --- |
| explain | Explain HITL/HOTL silent-reviewer waiting and same-agent retry; read-only | Correct rule and relevant evidence; no goal/FSM mutation, edits, tests or dispatch |
| plan-only | Plan README's in-memory create/list notes feature; cxc-loop named but execution forbidden | Useful file/verification plan; no goal/FSM, implementation, tests, commits or dispatch |
| c0 | Change README usefull to useful only | Exact byte replacement, fresh diff proof, unchanged HEAD and no new unit/goal/FSM/dispatch |
| interview-only | Ask one needed requirements question, not execution | Interview boundary retained; no goal or P/A/B/C/D, product edits, tests, commits or dispatch |
| c2 | Complete an existing internal in-memory notes slice in a disposable fixture | Route/service behavior and unchanged independent8-case oracle; relevant owners applied without loading all refs |

Order of first smoke pairs: explanation baseline→candidate; plan-only
candidate→baseline; C0 baseline→candidate; Interview candidate→baseline. These
are one pair per scenario, not statistical reliability or performance proof.

## C2 fixture concretization before its run

No public API, server, external storage, auth, permission or dependency changes.
Use an existing private synchronous request adapter, service stub and complete
in-memory store. Seed package.json, README, a normal existing numbered unit and
read-only test/notes.test.mjs. Only route/service and the existing unit record may
change. Existing tests specify create/list, trimming, Unicode, invalid-body and
blank-text rejection without insertion, path404 and method405. Run the oracle
before and after; retain source hashes and exact git diff. No commits, host goal,
FSM mutation or delegation are authorized in this ordinary C2 fixture.
Independent operator verification reruns the original tests and checks they were
not modified. This instantiates030's existing C2 acceptance row; no product scope
or completion criterion is reduced.

## First explanation pair: direct observations

Both recorder/analyzer exits0 with exact requested/effective Astra/high/priority.
Baseline thread01a070a6-deef-7211-8753-3046db25f5ad; candidate
01a070a7-e173-7c10-b7d6-99b85a169fb8. Native skill bodies appear in explicit
skill messages in the original rollout. Candidate's first command fully reads
waiting.md and its final answer includes both-mode applicability and retirement
consuming the same-agent retry. Independent behavioral audit is pending.

| Observable quantity | Baseline | Candidate |
| --- | ---: | ---: |
| Skill catalog plus loader-instruction message bytes | 9,485 | 8,192 |
| Wrapped explicit loop skill message bytes | 29,911 | 7,402 |
| Outer code-mode calls | 3 | 4 |
| Nested shell commands | 4 | 4 |
| Reported total input tokens, including cached input | 97,269 | 90,969 |
| Cached input tokens | 78,208 | 75,904 |
| Recorder elapsed ms | 44,269 | 39,611 |

Do not treat these messages as all cumulative provider input or infer exact
instruction tokens from bytes. Raw hook invocation count remains unavailable;
hook-attributed injected messages are separately visible. One pair cannot establish
latency improvement. Candidate made an additional outer round trip and a failed
lookup of repository-only structure/20_pabcd_dispatch_doctrine.md. The file is
not installed in either variant.031 deliberately retains it as repository
provenance, not a new runtime dependency; the live wording may need a clearer
label to prevent futile lookup. No missing-source success or zero retry claim.

C0 pairs each emitted the identical nonfatal startup warning for the enabled
under-development default_mode_request_user_input feature. It remains visible;
no warning suppression or broad item-error exemption was added to the analyzer.

Remaining gates: independent behavior/read-coverage audit, C2/common-path proof,
fresh C receipt, WP3 integrated arming behavior and WP4 exact-candidate full-cycle
cost/installation evidence. Cheaper failed or incomplete execution is not a win.
