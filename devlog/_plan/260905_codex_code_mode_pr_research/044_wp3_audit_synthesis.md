# WP3 A audit synthesis — do not build rejected plans

Status: A FAIL at2ee7708; product implementation has not begun.
Reviewers: Cicero01a070ec-3d65-79f3-b441-ce59e6561f20 (scope/native feasibility),
Leibniz01a070ec-3e12-7610-b68c-249b16a9a535 (family evidence).

## Main accept/rebut decisions before repair

All five findings are accepted. They reinforce the user's agent-owned modular
direction; none is traded for smaller output or a passing benchmark.

| Finding | Root cause / conflict | Main repair decision |
| --- | --- | --- |
| Scope High1: N17/N18 cannot revisit the same thread using fresh-only record() | Separate setup run has a different goal identity; schema2 explicitly excludes resumed history | Design a bounded native exec-resume protocol and exact recorder/evidence changes in045 before B. Actual CLI0.146 resume help has been read. Preserve strict model/effort/priority, isolated same-home history, unique output artifacts and no direct goal DB edits. Native preparation must be demonstrated; no pretend preconditions. |
| Scope High2: natural P/I hints still mutate phase under no-FSM | Treating real phase/orchestration changes as harmless bookkeeping contradicts loop's agent-owned transition contract | Expand the named hook.ts scope narrowly to REMOVE automatic phase entry from the natural-language branch. Keep lexical advisory detection, explicit command parser, all legal-edge/attest/identity/source/receipt guards, and ordinary bootstrap/dedup. Actual phase entry belongs to explicit user commands or authorized agent CLI calls. No permission classifier or new toggle. Update affected tests/SoT and add plain P/I no-FSM negatives plus authorized explicit-entry positives. |
| Scope High3: loop-first N8 does not exercise I, post-answer directive independently orders work | Reachability gap plus a second instruction surface outside R1's local qualification | Add loop-token-free I/no-delegation and post-answer cases. Scope the existing RESCAN_REINJECT_DIRECTIVE text independently; preserve capture, firewalls and readiness checks. Required but forbidden actions remain unmet, not skipped proof. |
| Evidence High1: native V1/single-turn claims omit context identity/version checks | Metadata/start/end checked, actual context only model/effort | In schema2 validate every native-shaped parent/child context turn_id against start/end and actual multi_agent_version V1; add missing/mismatched/V2 cases. Preserve schema1 behavior. |
| Evidence Medium2: extra declared source bytes are never revalidated | Review binds declared hash values but unused source can change later | Validate every declared source in schema2 (or explicitly reject unused declarations); add unchanged-review tamper and missing-extra-source tests with rc1/rc2. No new collector or weakened provenance. |

The natural-entry removal changes a heuristic's action, not a protective guard.
It is explicitly part of this amended plan under the user's authorized hook/agent
scope, not an unreviewed implementation deviation.042's original string-only
restriction is superseded only for that exact branch and affected expectations;
all other runtime mechanisms remain out of scope. Sources are not edited in A.

Dependencies: revise042 and043, finish045's grounded same-thread protocol, fold
back to the same independent reviewers, integrate040 and only then consider B.
Existing mandatory cases, historical failures, F7/full-history and supported V2
delivery requirements remain. No criterion or source/binary identity claim is weakened.

## Partial independent fold-back

Leibniz passed revised043 at plan level: actual context turn/V1 validation and
all declared source byte validation now have reached-branch negative fixtures.
Cicero accepted the narrow natural-entry removal and independently scoped rescan
text/test design. Its Stop-at-IDLE wording correction is also folded into042.
Neither verdict permits B while045's native same-thread/pending-answer protocol
is incomplete. No proposed implementation or regression result is claimed here.

045 feasibility now has a real native pending question/response/PostToolUse and
completed turn, plus same-thread paused-goal inspection through first-party APIs.
The separate CLI resume timeout remains a failed specimen. All temporary native
server processes used so far have exited; exact protocol and acceptance audit remain.

045 now contains the complete proposed fixed operator/RPC sequence and independent
acceptance path, with actual callback and paused same-thread inspection evidence.
The source-backed supplemental app-server route supersedes the originally proposed
product-recorder extension: exec remains primary, no new shipped recorder/runtime.
Cicero is reviewing this final High1 dependency. Actual N17 active admission,
genuine blocked preparation and I/post-answer behavior still require execution;
neither a proposed recipe nor a timeout specimen is a native acceptance PASS.

## A closure

Cicero completed045 review atb11f567b and returned PASS with no remaining plan
blockers. The first remaining execution gate is actual N17 get_goal=active and
IDLE before mutation; invalid ordering stays INVALID PRECONDITION, not presumed
success. Together with Leibniz's family-only plan PASS, all five accepted A
blockers are resolved at plan level. Main accepts the combined A verdict and
authorizes only the amended040/042/043/045 B scope. Every native behavior, C
receipt, full-history/V2 and final adoption obligation remains open.
