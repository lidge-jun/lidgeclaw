# WP3 C repair: live-schema Mind dispatch

Status: native004 RED; native005 behavioral GREEN independently reviewed by Pauli.
The old-shape-only native causal toggle was not executed. Native005's wrapper
ended by deadline; exact outbound effort and scheduler priority remain unknown.
Fresh current-product affected checks pass703/0; see [research wrap](049_1_research_wrap.md).
No whole-WP3 completion is claimed. This repairs the existing instructional
hook/owner compatibility, not050's inline/selfload delivery mechanism.

## Symptom, competing explanations and accepted boundary

At fb7c39d, candidate-n20-answer-004 uses the exact disclosed admission-v2 pair.
N20-I now performs actual status(IDLE) before CLI I, then real question/answer/R7
capture and correct I footer. N22 explicitly permits read-only Minds and requires
Astra/high. Its completed final at RPC485 refuses dispatch because the exposed
V1 tool lacks agent_type/task_name demanded by MIND-SPAWN-SHAPE-01.

H1: old version-specific argument requirements are treated as universal.
Observed: minds.ts:76–82 injects them at developer priority and interview SKILL
206–216 repeats them. Falsifier: schema-adaptive owner/hook still cannot dispatch
the same authorized original admission request, or old-shape-only toggle does not
restore the refusal. Keep native004 and all future failed attempts.
H2: Astra/high is unsupported. The actual exposed schema and final explicitly
support both; reject this explanation for004, but still verify every new run.
H3: dispatch permission/goal firewall is absent. New N22 explicitly grants Mind
dispatch and tracker/scan writes in a real no-goal I session; reject that explanation.
Original002 already dispatched by embedding role labels, albeit at low effort.

Pauli's read-only design verdict PASS: both hook and owner need correction;
owner-only guidance cannot safely override a higher-priority hardcoded shape.
No dispatcher, parser, role inference, guard or goal behavior needs modification.

## Exact ownership / afterimage

MODIFY existing pabcd/references/delegation.md: one canonical live-schema transport
section. Preserve logical explorer/worker role intentions. Supply a field only
when the loaded tool schema exposes it; missing role/lens fields become explicit
task-message labels and scope, not invented tool arguments or claimed native
enforcement. Track actual returned handles (V1 agent_id or V2 canonical task_name
as exposed), never use the requested display label as a native ID. Preserve actual
runtime evidence obligations; a prompt label cannot evade a worker receipt gate.
Supported controls and user constraints win; no role/profile/config mutation.

ADD interview/references/mind-dispatch.md: the existing Mind-only non-full-history
fork, config/inheritance caveats and packed snapshot obligations, plus a link to
the generic transport owner. Read only when actual Mind dispatch is authorized.
Preserve all five lens semantics, lowest-dimension selection, cap3, independent
contradiction results, actual returned handles, scope and readiness evidence.
Version fields are conditional on live schema. Fixed model/effort/tier constraints
must not be called harmless when role inference picks another configured role.

MODIFY interview/SKILL.md: replace the long MIND-SPAWN-SHAPE paragraph with that
conditional reference route. This decreases unconditional body size while adding
the missing compatibility detail; it does not delete knowledge or modules.

MODIFY minds.ts MIND_DISPATCH_DIRECTIVE only: replace the hardcoded spawn/effort
paragraph with a short authorized-only reference/schema pointer. Keep main/leaf,
read-only, lowest-dimension cap and snapshot requirements. No functions or lens
data change. Update its comment and hook.ts's emit-resolution comment after the
configuration example moves into the reference.

MODIFY minds.test.ts only at the literal spawn-shape payload test: runtime pointer
and scope/non-full-fork contract, resolving its reference path to a real packaged
resource. Do not add Markdown phrase assertions. Preserve all selector/normalizer/
guard and unrelated V2 transport fixtures. REGENERATE dist/minds.js and dist/hook.js
only using the remote build. Sync structure20/other direct SoT where role fields
were stated unconditionally, without rewriting unrelated doctrine.

## Verification / continuation

Class: bounded C3 instructions with C4 scrutiny of role/evidence boundaries.
Search terms: MIND_DISPATCH_DIRECTIVE, MIND-SPAWN-SHAPE-01, agent_type, task_name,
fork_turns, cxc subagents set explorer. Reuse existing source/owner/reference/test
structure; no new production runtime, helper adapter, registry or default flag.

Use macmini only: selected minds/hook/continuation/interview/spawn/compiled/catalog
tests plus build/gate, then the broader affected selection. New native005 must use
the same admission-v2 bytes as004 and prove full owner/reference delivery before
supported-argument dispatch, actual returned handles/settings/results and real
scan record. Retain scope-negative first turn. A V1 PASS is not V2 certification;
050 still owns native delivery variants. Keep resource/privacy/teardown controls.
Record RED/GREEN and old-shape-only toggle evidence before causal completion.

## First implementation proof

Pauli independently reviewed the actual source/reference/test/SoT delta and
returned PASS, no blockers. No dispatcher/selector/normalizer/role-inference or
guard function changed. New Mind reference49 lines; generic transport owner67.
The Interview body and injected Mind payload become shorter while conditional
detail increases, matching the user's modularity/knowledge-preservation principle.

Remote wp3-source build succeeds; selected minds/hook/continuation/interview/
spawn/compiled/catalog checks:290 pass,0 fail/skip/todo,6998ms. Original native004
RED remains. These checks prove packaged routing and preserved deterministic
logic, not native005 success. R/wp3-mind-build.log and wp3-mind-check.log retained.
Generated minds.js/hook.js are copied from that build, never hand-edited locally.
