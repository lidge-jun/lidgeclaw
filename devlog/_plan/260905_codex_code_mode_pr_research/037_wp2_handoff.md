# WP2 check handoff — modular instruction candidate

Status: WP2 C/D closed. Fresh source91e051df verification passed118 tests, gate,
inventory, route mutations and three native packet rechecks. The archived receipt
is .codexclaw/evidence/01a0702d-c493-7510-801f-7d8772a2689c/wp2/test-receipt.json.
This is not global adoption or release; WP3 A and downstream gates remain open.

## Implemented slice

Core loop/pabcd/dev entrypoints route to owned references. New waiting and plan
output modules preserve mode-neutral lifecycle and complete C2+ P-plan fields.
Both nested and outer output budgets are named; truncated files are reread without
guessed ranges. Reference count and stored knowledge are not reduction targets.
Rules, eight implicit identities, frontend/uiux, runtime hooks and dist are preserved.

Independent source review and fold-back passed each amended slice. Remote targeted
suite repeatedly passes118/0/no skips with gate/inventory0, and missing route,
renamed target and empty-reference mutation controls each fail as expected.

## Bounded actual behavior evidence

- Explicit explanation, C0 exact typo and Interview restriction observations:
  033/034 and original001 packets; historical baseline delivery failures retained.
- Repaired candidate plan-only002: nine concepts actually present, complete selected
  material delivered and no prohibited actions; independent actual-behavior PASS.
- Repaired explanation002: mode-neutral wait rule applied with no missing repository
  lookup; actual-behavior PASS.
- Candidate c2-neutral002: all selected required material delivered before editing,
 8 unchanged oracle cases pass and only3allowedpaths change; no CHECK/dispatch.
  Independent complete-read/behavior PASS and separate main oracle rerun.

These establish the skill-owned contract and representative conditional reading
slice. They do not establish universal model adherence or a robust performance
improvement. Failures and costs are retained, including higher observed C2 input.

## Mandatory downstream work, not waived

1. WP3: revalidate040's CHECK pointer before building. Both original Korean C2
   requests containing a natural verification phrase receive a higher-priority
   directive that orders review despite no-delegation. The skill cannot override
   it. Preserve deterministic safety/state/evidence mechanisms while returning
   procedural choice to scoped owners. Repeat the original prompt, not neutral only.
2. WP3/050 measurement:035 proves native root/child session identity differs from
   thread identity and OCX rows are grouped. Extend the audited observation contract
   or use a truly observed finer source before claiming per-child wire attribution.
   Do not fabricate child conversation IDs, duplicate family cost or change OCX.
3. WP3/4: actual bare-loop HOTL arming, full-cycle behavior, all specified negative
   paths, repeated successful common-path cost comparisons and mandatory self-load
   versus inlining trials remain required. WP2 was never an independently releasable
   runtime-default change;030 explicitly names this dependency.
4. WP4: final exact payload install/cache identity, generated runtime proof and SoT/
   docs-site synchronization, independent final review and complete goal criteria.

Local current source payload equals the tested d8c63f0 candidate; subsequent commits
only record evidence. No push, release, shared-runtime switch or goal completion.
The original native host goal still reports blocked and exposes no resume method;
explicit user-authorized work continues via the existing FSM/goalplan without
faking native DB state or claiming Stop automation is armed.
