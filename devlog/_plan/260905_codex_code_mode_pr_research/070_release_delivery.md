# Authorized delivery after the research wrap

Class C4, satisfy-spec release/integration work. Trigger: the user explicitly
authorized finishing the PRs, release, and application to all SSH installations
on2026-09-06. This supersedes the earlier publication/deployment prohibition.
The practical outcome is the collected candidate integrated with current dev,
then a verified immutable release and existing-install upgrades. No renewed
open-ended methodology research, unrelated PR action, native runtime replacement,
credential expansion, app termination, or worktree cleanup is included.

Evidence and checkpoints remain in this unit and the existing session evidence
directory. No token/total-time budget was set. Individual commands stay bounded;
macmini owns tests/builds, GitHub owns CI/release, SSH hosts receive only the
verified artifact. Main owns integration and external writes. Read-only inventory
lanes are Lagrange (SSH) and Gauss (GitHub/release). An unresolved security defect,
failed named gate, inaccessible installed host, or ambiguous install identity
prevents claiming that affected delivery complete. Never manufacture a PASS.

## Observed source and integration boundary

- Research HEAD: f0a141a5633a809e9139d9a58f9ed716d98cc0ab, clean.
- Current dev:336d247962e4f7c9b87332d6e132295ad68f03f0;38 commits beyond main.
- Main/latest v0.2.16:048ae759c715051f2fde807624e85cf1ec6c6d55.
- No open PR and no existing PR for the research branch at inspection.
-13 plugin paths overlap: hook/map source+dist+tests, dev/scaffolding ownership,
  interview, loop and pabcd. Dev's peer collaboration, browser routing, native
  stack awareness, license attribution and other accepted changes must survive.
- Existing GitHub enforcer requires feature PR -> dev, then dev -> main promotion.

MODIFY by integration, not overwrite: merge origin/dev into the current managed
branch, preserve both parents, resolve each source/skill conflict semantically.
Preserve conditional reference ownership from this branch and carry dev's newer
policy changes into those references. Rebuild generated components remotely from
the resolved source; do not resolve dist independently of source.
Source targets are the13 intersecting plugin paths identified above plus
structure/20_pabcd_dispatch_doctrine.md and structure/INDEX.md (15 total).
Preserve dev's peer/stack guidance and this branch's schema-adaptive dispatch and
scoped-hook descriptions together; inventory and public
test counts are generated from the integrated full-suite result, not the703 subset.
Where the research moved a dev policy out of SKILL.md, carry the dev interdiff to
its existing destination: dev/references/development-practice.md (size/ESM),
methodology-overlays.md (stack routing), loop/references/waiting.md (peer boundary),
pabcd/references/delegation.md (live discovery/model authority), phase-audit.md
(optional native role field), and phase-check.md (docs-only verification).
No policy is dropped merely because Git reports a modify/delete-style conflict.

Initial full-suite check on f0a141a, Node22.22.0:2521 tests,2513pass,0fail,
7cancelled,1skip; exit is not PASS. All cancellations are in existing messenger
media/rate-limit timeout tests with pending promises after event-loop completion.
Inspect their actual implementation and CI runtime before attributing cause.
Preserve the failed run. npm ci also reports2 high advisories: inspect the audit
before release, do not silently apply npm audit fix or waive the findings.

## Publication plan

Use a cohesive integration PR for the final skill/hook contract: historical
intermediate skill revisions have documented incompatible old-hook behavior and
are not independently safe release layers. The large evidence history is retained
as provenance, not replayed as separate feature PRs. The version-only release
preparation is separate after integration; no speculative stack registration.

For each PR: explicit dev base, exact head checks, independent source review,
resolve actual review findings; merge without deleting a live/used branch.
Then promote dev -> main, wait for resulting main-SHA CI/packed/WSL success.
Read current status immediately before every merge and publish.

MODIFY for version0.2.17 (unless a concurrent release takes it): package.json,
package-lock.json workspace versions, cli/package.json, eight component package.json
files, plugins/codexclaw/gui/package.json, plugin manifest base+fresh cachebuster,
and generated inventory/readme/docs count blocks. No dependency changes merely
for a version bump. Any vulnerability fix is separately reviewed and tested.
Do not change docs-site's independent0.0.1 version.

Run the existing Release workflow dry-run and publication on the frozen main SHA.
It performs suite, inventory, embedded versions, build/dist consistency, exact-SHA
CI/packed evidence and release-manifest verification before publication. Re-read
the published tag, SHA, tarball, SHA256SUMS and candidate manifest afterward.
Verify both workflow runs' actual head SHA equals the frozen release SHA. If main
moves, re-establish exact-head gates. An occupied version is a stop/reconciliation
condition, not permission to overwrite existing release assets.
No npm publishing: root package is private and the canonical artifact is GitHub's
payload archive. No direct publication around a failed workflow gate.

## SSH rollout contract

Inventory named SSH aliases, deduplicate user/host identity, distinguish actual
production installations from probe homes and checkout-only conveniences.
Record preimage manifest/path/digest and supported normal installation mechanism.
For reachable existing installs: preserve configuration and trust data, retain
rollback payload, install the pinned verified candidate through supported CLI,
then verify normal hook trust, resolved path/version/content digest and a bounded
installed smoke. Existing sessions may retain loaded instruction/tool snapshots;
do not claim they hot-reloaded or kill an unrelated application to force pickup.
Unreachable hosts remain explicitly not deployed, never silently omitted.

## Evidence versus claims

The original research's unmet optimization/delivery-comparison criteria remain
recorded as unmet. A release check does not retroactively complete those studies.
049_1 retains the dated wrap and its known failures/unknowns. This delivery's
authority is current, but success still needs actual integration, exact-head
checks, publication and per-host applied-state proof. No speed/cost percentage,
fewer registered hooks, or scheduler priority guarantee will appear in the release.

## Independent preparation audit

Gauss: GO-WITH-FIXES, two medium corrections accepted above (15-path scope and
actual workflow SHA/occupied-version guard). No further research gate is required
to perform the authorized local integration. This does not certify the original
research studies. Main judgment: near-pass; both corrections folded before merge.
Node24.20.0 with the same source passes the previously cancelled24 timer tests,
0cancelled; the Node22 failure remains a separate runtime-specific observation.
