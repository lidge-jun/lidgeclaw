# WP3 preflight and current baseline

Status: P revalidation; no hook/evaluator implementation in this cycle yet.
Previous D:037 locks the skill/reference candidate but explicitly retains Korean
CHECK authority conflict, shared-family attribution and all integrated/cost gates.
Current cycle objective remains scoped instructional reduction with unchanged
mechanical safety/state/evidence boundaries, not a new execution runtime.

## Fresh verifier grounding

All execution is on macmini-cf. Baseline is clean source91e051df, whose plugin
payload is identical to tested d8c63f0 (main git diff over plugins/codexclaw exits0).
Local work is source/doc inspection, editing and checkpointing only.

- Direct source tests for hook, hook-continuation, map-affordance, goal-gate,
  session-split, interview-ledger and spawn-attach:284 PASS,0 FAIL,0 skip,
  command exit0 (wp3-P-baseline.log). Each path is a direct node --test argument,
  so the prospective verifier actually reads the touched handlers/callers.
- In a separate owned wp3-build-baseline clone, npm run build exits0 and compiles
 156 files using existing node:module type stripping, then validates manifest
  targets. No external toolchain/dependency installation. Git status remains
  clean after generation, so this Node22.22.0 baseline has no generated drift.
- Compiled hook-e2e.test.mjs after that build:28 PASS,0 FAIL,0 skip, exit0
  (wp3-P-build.log, wp3-P-e2e.log). This directly invokes real compiled entries;
  it is not proof of native hook trust or model adherence.
- Installed-root synthetic benchmark is being retained separately. Generic replay
  cannot establish matcher fire rate, actual no-work or model instruction exposure.

Installed baseline replay completed:23 hooks ×20 iterations,0 errors,88,364 raw
stdout bytes,0 stderr bytes; common harness9193170579d2e08b78f16b11a52a44eb6914484c2714839b696f550709a43e88.
This is still synthetic protocol output, not exact model context or scheduler proof.

## Main pre-audit synthesis

042's first draft addresses CHECK but needs literal scope corrections before A:
no-tests must not erase a separately authorized build/typecheck; no-goal/no-FSM
must not prohibit read-only inspection; resumed matching goals must be reused,
not recreated. Literal-only phase text byte inspection also found P/B/D grew
(baseline I/P/A/B/C/D1883/565/1381/326/1070/512; first proposal
1057/578/800/676/882/621). Author is compacting the repeated boundary rather than
claiming universal reduction. No proposed code was executed for this byte count.

043's first draft must call run.codexSha256 the invoked Codex entrypoint/launcher
hash, not a Rust binary identity. Its legacyV1/direct-child/no-fork coverage does
not satisfy pending050 full-history or other supported-surface rows. Unknown
coverage stays unknown; these corrections do not add a logger or change OCX.

Author fold-back is complete:042 revised phase constants are750/362/494/295/608/373
bytes; arming text grows for inspect/reuse/new-goal distinctions, so no blanket
emitted-context or latency claim.043 now correctly names codexEntrypoint and
keeps full-history/V2 trial obligations outside its supported proof mode.
Independent A audit still needs to check implementation feasibility and these
scope/measurement boundaries; authors' completed plans are not implementation PASS.

These runs ground040's verifier commands on the current source. No source changes
were made in those baseline checkouts. Current authored amendments042/043 will
receive independent audit before B. Do not treat their creation as execution.

## Active boundaries

Scope/intent instructions may become shorter pointers, but guards, phase/ledger
transitions, session binding, trust, worktree protection and evidence validation
remain mandatory. Exact natural-language CHECK reproduction and negative task
limits remain in native acceptance; neutral control is supplementary, not a substitute.
Shared-family measurement must validate real causal metadata and deduplicate costs;
per-child request attribution remains unavailable unless actually observed.

No local suite, production switch, push, release, new goal, native DB edit or
permission bypass beyond the approved disposable codex exec probes.
