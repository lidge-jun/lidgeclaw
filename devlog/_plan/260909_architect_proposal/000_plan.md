# Architect proposal publication

Status: PROPOSAL

One scoped HOTL cycle, proposal. Previous D at ec00fa2 fixed native registration; final review found routeDispatch lacks agent_type. Jun now authorizes fork push and Draft PR, asks proportionate review because adoption is uncertain. No host installation, provider call, VM setup, global mutation, merge or other-branch changes. Minimal fixture checks/build allowed by explicit clarification; no broad repeated suite.

Change: MODIFY components/subagent-config/src/spawn-wrapper.ts routeDispatch return type adds agent_type: SpawnPayload["agent_type"], return object uses ROLE_AGENT_TYPE[role]. Modify existing design-intent test to assert actual native type, send the returned spawn fields (excluding local role metadata) through hook and assert architect type/model survive. Add mapped type expectations to existing intent tests where appropriate. MODIFY spawn-attach-hook.ts stale comment architect/explorer -> native architect, legacy markers not permissions. Build tracked dist. No runtime role-registration changes. Main owns tightly coupled patch; independent bounded A review. Full feature static review uses prior exact reviews plus the current contract/consumer pass, no new install proof.

Verified native/source binding; clean ec00fa2. origin/dev fetched and still6e97e73; README contributing targets dev. Personal fork thisisjun786/codexclaw verified. No project AGENTS/POLICY/PR template found. Existing PR91 targets dev, head9a546f0, overlaps registrar/CLI/executor header; Draft body must name reconciliation requirement, no hidden dependency or merge. Publish one cohesive proposal, no native stacks. Include design motivation, main->architect proposal->main plan->reflection->independent reviewer, setup requirement, source-bound validation vs native gaps and historical screenshot scope.

Verification: source/diff review, existing wrapper test (direct target, previous invocation succeeded but exposed omitted-type test gap), npm run build (component source enumerator previously verified), git diff --check and source-bound C receipt. Existing full suite2658pass71skip at afcbb2f is historical evidence only; do not claim fresh full suite. No host model discovery/read-only enforcement/provider proof. Draft PR rather than ready-to-merge claim. Prepublication diff and authorship inspect; push non-force to personal fork; create ordinary Draft targeting upstream dev; verify URL/head/base/state. Close D after real artifact publication evidence.


B/C record: independent A reviewer01a08474 PASS. Accepted direct field assertions and routeDispatch JSDoc update. New assertions fail prepatch on undefined agent_type; targeted wrapper suite passes after fix (green.log), build exit0. Entire proposal diff reviewed against origin/dev6e97e73: role/store/API/GUI/parser chain, registration preservation, native identity, readonly hook gate exclusion, explicit install prerequisites, doctrine and current references. Prior independent runtime/native reviews retained at their revisions. No new high-risk behavior outside native field addition; tests, historical devlog and generated dist accounted for. Historical settings screenshot copied from verified fixture run; GUI source unchanged since its capture at6db5134, no claim of native inference. No broad full-suite/install/VM run for this proposal pass.

Publication thesis confirmed by Jun: architect adds domain-specific model selection within review work. PR destination is ORIGINAL lidge-jun/codexclaw dev; personal fork only supplies the head branch. Draft, no merge. PR91 overlap includes semantic difference: worker fallback is safe for executor; alias fallback defeats independent architect identity and is deliberately absent.

Final publication preparation: fbb9c43 targeted wrapper32 pass, build0, diff check0. All public diff files accounted for (runtime/settings/GUI/workflow docs inspected, test deltas reviewed, dist regenerated, historical records retained, screenshot visually inspected in original capture). Proposal unit remains under _plan while upstream adoption is undecided; this is not an installed/runtime-proven feature claim. PR URL and publication proof are recorded in the native session goalplan.
