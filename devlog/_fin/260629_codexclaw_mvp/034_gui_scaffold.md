# 034 — GUI Scaffold

Status: TODO  ·  Phase 2

## Goal
Local web dashboard (Vite + React), mirroring the opencodex GUI stack for familiarity.

## Scope
- App shell, routing, dev server, `codexclaw gui` launcher (bin/CLI).
- Reads/writes via the subagent-config component.

## Reference
opencodex `gui/` (Vite + React; pages Providers/Subagents/Usage/Logs/CodexAuth).

## Verify
- `codexclaw gui` serves the dashboard; loads with no console errors.
