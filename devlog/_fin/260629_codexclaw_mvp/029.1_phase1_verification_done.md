# 029.1 — Phase 1 Verification Gate (029) — DONE notes (Pass 7 C)

Status: DONE · Phase 1 · Loop Pass 7/7 (final). Evidence captured `node --test` / `node build.mjs` on Node v24.14.1.

Closes the 029 S1–S5 gate. Each item is proven by an offline artifact (no network, no real `~/.codex` mutation). The S-item scoping follows 070.1 REV2 (A-audit Confucius corrections folded in).

## S1 — install + skill discovery
- `scripts/build.mjs` validator: `build OK — 12 files compiled, layout validated.`
- Validator confirms `skills/` present and every skill dir has `SKILL.md`; 13 skill dirs (dev hub + 11 routers + pabcd).
- Frontmatter validity vs codex-rs `core-skills/loader.rs` was C-gate-verified in Pass 4 (name ≤64, description ≤1024, metadata.short-description present).

## S2 — IPABCD directive injection (compiled artifact)
- `build.test.mjs` runs the COMPILED `pabcd-state/dist/cli.js hook user-prompt-submit` with an explicit interview-trigger payload in a temp cwd.
- Asserts: (a) `hookSpecificOutput.additionalContext` carries the `[codexclaw: INTERVIEW]` directive; (b) the per-session state file `<cwd>/.codexclaw/sessions/<id>.json` is written with `orchestrationActive:true` + `lastInjectedPhase:"I"`.
- SCOPE (070.1 REV2 / A-audit blocker 2): the Pass-2 hook proves directive-injection + orchestrationActive state write — it does NOT mutate `state.phase` and writes no ledger at runtime (handleStop is a deliberate no-op, hook.ts:158/205). FSM phase-advance + ledger are exercised by pabcd-state node:test (52/52); runtime auto-advance is an explicit Phase-2 deferral, not a Pass-7 gap.

## S3 — pilot dev skill routes
- `dev` hub: `agents/openai.yaml` `allow_implicit_invocation: true` (only always-on skill).
- 11 routers + pabcd: `allow_implicit_invocation: false`/absent (on-demand, description-triggered).
- Spot-checked frontmatter in Pass 4 C-gate (Gibbs path) + re-verified this pass.

## S4 — config.toml byte-identical (027 guard)
- `config-guard` node:test 15/15 green — proves offline backup/revert + sha256 drift guard with an injected codexHome and `assertNotRealCodexHome` real-path guard, so the real `~/.codex` is never touched.

## S5 — subagent config is spawn-valid on default model
- A-audit blocker 4 wording: codexclaw injects roles via `spawn_agent({agent_type, message})` at runtime (plugin manifests have no agents field — Pass 5 finding). The provable offline artifact is that each `agents/*.toml` parses with `model` unset/default and the README inline-injection contract holds (Pass 5 B). A live spawn is a host-runtime action outside this offline gate.

## build aggregation reproducible
- `build.test.mjs` idempotency test: two consecutive builds produce byte-identical `dist/` across all 4 components.
- `.ts`→`.js` specifier rewrite asserted (no `.ts` relative specifier remains in any compiled `dist/*.js`).

## no `[TODO]`/placeholder markers
- Validator + `build.test.mjs` scan `plugin.json` AND every component `src/**` + compiled `dist/**` for `[TODO]`/`TODO(`/`FIXME`/`TBD`.
- `provider-bridge/src/cli.ts` literal `TODO(...)` removed (neutral Phase-2 note instead); shipped as graceful exit-0 SessionStart no-op.
- `subagent-config/src/mcp.ts` upgraded from bare `exit(0)` to a minimal valid no-tool stdio MCP server (completes `initialize` + `tools/list` empty), so the `.mcp.json` reference is honest. Verified handshake in `build.test.mjs`.

## Test totals (fresh, this pass)
- `npm test` → 73/73 pass (pabcd-state 52 + config-guard 15 + build 6).
- `npm run build` → 12 files compiled, layout validated, exit 0 (ExperimentalWarning for stripTypeScriptTypes is expected + tolerated).

## Out of scope (documented, not gaps)
- GUI build (Phase 2); marketplace remote `codex plugin add` execution (commands documented, not run against network); provider/ocx runtime (Phase 2); Node downlevel (target pinned Node 24).
