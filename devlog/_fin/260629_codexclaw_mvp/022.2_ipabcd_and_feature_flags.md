# 022.2 — IPABCD + Feature-Flag Activation

Status: RESEARCH+DESIGN (source-verified)  ·  Phase 1
Decision (jun): The workflow is **IPABCD**, not IPABCD — the **Interview** phase is included.
Activation is via codex feature flags written to config.toml at install (`codex features enable`),
which jun documented as the intended "turn the flag on" mechanism.

## Sources (MUST cite when building)
- `codex features list` (live, this build) — feature stages/states.
- codex-rs: `features/src/lib.rs`, `core-skills/src/loader.rs` (021.1), `core/src/agent/role.rs` (015.1).

## Feature flags relevant to codexclaw (verified via `codex features list`)
| Feature | Stage | Default | Use in codexclaw |
|---------|-------|---------|------------------|
| `multi_agent` | stable | true | subagent (worker) spawn — Phase 1/2 |
| `goals` | stable | true | goal system + create_goal gate (023) |
| `hooks` | stable | true | IPABCD hook triggers + PreToolUse gate |
| `default_mode_request_user_input` | under development | true | **Interview phase input** (`request_user_input` tool) |
| `skill_mcp_dependency_install` | stable | true | skill MCP/tool dependency (openai.yaml dependencies.tools) |
| `multi_agent_v2` | under development | false | future subagent upgrade — do NOT depend on it |

## Activation mechanism (resolves config-untouched tension with 027)
- Official path: `codex features enable <name>` writes `[features].<name> = true` into
  `~/.codex/config.toml` (equivalent to `-c features.<name>=true`).
- This is a SANCTIONED install action, not silent mutation. So 027 is revised:
  - 027 "config untouched" now means: codexclaw does NOT arbitrarily edit unrelated config; it ONLY
    flips the specific, documented feature flags it needs, via the official `codex features` CLI,
    and only at install/enable time with user awareness.
  - codexclaw should back up config.toml before flipping, and `codexclaw uninstall` should revert
    exactly the flags it set (mirror opencodex's `ocx stop`/restore discipline).

## IPABCD phases (I added)
- **I — Interview**: clarify requirements via `request_user_input` (feature
  `default_mode_request_user_input`). Mirrors cli-jaw `cli-jaw orchestrate I`. Entry: ambiguous/
  large request, or explicit "interview me". Produces confirmed requirements before Plan.
- **P/A/B/C/D** as in 022 (Plan/Audit/Build/Check/Done).
- State (`.codexclaw/sessions/<sessionId>.json`, per-session — see 016) gains phase `I` and an `interview` flag; transition I→P only after
  requirements confirmed (mirror this very interview's Ready criteria).

## Interview trigger porting (from cli-jaw)
- cli-jaw `cli-jaw orchestrate I` / `/interview` → codexclaw: UserPromptSubmit hook detects
  interview triggers ("interview me", "기획부터", vague+large) and the `pabcd` (→ rename mental
  model to `ipabcd`) skill drives `request_user_input` rounds.
- Keep cli-jaw's 4-dimension interview model (goal/constraint/success/ontology) as skill guidance.

## Open items
- Q-IPABCD-1: `request_user_input` exact tool contract in this build (single vs multi question,
  options schema). Verify against the tool handler before building the Interview skill.
- Q-IPABCD-2: Should `default_mode_request_user_input` (under development) be a hard dependency,
  or should Interview degrade to plain Q&A text if the flag is off?
