# 024.1 — Conversion Recipe Anchor (dev-debugging as example router)

Status: TODO  ·  Phase 1

## Scope correction (jun, 2026-06-29)
- Phase 1 ports **ALL 13** dev skills. debugging is NOT a special "deliverable pilot" — it is just
  ONE example of a dev skill acting as a SUBAGENT ROUTER role.
- All dev-* skills are routers: a subagent doing debugging work routes through `dev-debugging`,
  frontend work through `dev-frontend`, review through `dev-code-reviewer`, etc. (mirrors cli-jaw's
  always-on `dev/SKILL.md` that routes by change-surface to role guides).

## Why convert dev-debugging FIRST (build tactic only, not scope limit)
- It is the recipe anchor: convert one end-to-end to lock the exact conversion steps, THEN bulk-port
  the remaining 12 with the same recipe. This is sequencing, not a reduced Phase 1 deliverable.

## Recipe steps (applied to ALL 13)
1. Copy cli-jaw `/Users/jun/.cli-jaw-3459/skills/<dev-skill>/SKILL.md` (+ references/).
2. Apply 024 conversion rules (frontmatter schema, strip cli-jaw plumbing, keep universal discipline).
3. Add `agents/openai.yaml` if routing/interface metadata benefits.
4. Install locally; verify it routes on a representative prompt for that surface.
5. Capture the dev-debugging diff as the canonical recipe; replay across the other 12.

## Verify
- dev-debugging routes on "this is failing, find the root cause"-style prompts.
- Recipe reproduces cleanly on a second skill (e.g. dev-testing) before bulk port.
- Final Phase 1 state: all 13 converted, validated, zero cli-jaw paths.
