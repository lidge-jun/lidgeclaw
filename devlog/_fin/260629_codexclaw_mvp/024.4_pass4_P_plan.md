# 024.4 — Pass 4 (P): dev-* Skill Conversion (all 13) — execution plan

Status: P (PLANNING) · Phase 1 · Loop Pass 4/7 (see 017) · executes the 024.3 delta
Unit: T-024 — convert 12 cli-jaw dev-* skills + fold dev-pabcd into `pabcd`, as Codex skills.
Grounded: codex-rs core-skills/loader.rs (verified) + omo skill exemplars + 024.3 delta.

## Scope boundary
- IN: create `plugins/codexclaw/skills/<name>/SKILL.md` (+ `agents/openai.yaml` where policy needed, + `references/*.md` where the source has them) for all 13 per 024.3 §2/§3/§5. Strip every cli-jaw-ism (C1-C10). `dev` = always-on (allow_implicit_invocation:true); 12 surface skills = on-demand routers; `pabcd` absorbs dev-pabcd discipline.
- OUT: subagent role .toml wiring (Pass 5), Stop-hook evidence gates / FSM auto-advance (Pass 5), install/activation (Pass 6), build aggregation + 029 gate (Pass 7). No source-code (.ts) changes this pass.

## Ground truth (verified this phase)
- `SkillFrontmatter` (loader.rs:38-48): only `name`, `description`, `metadata.short-description`. Limits: name ≤64, description ≤1024, short-description ≤1024 (loader.rs:112-114). `keywords`/`triggers` NOT read → DROP.
- Sidecar: `skills/<name>/agents/openai.yaml` (loader.rs:109-110, SKILLS_METADATA_DIR="agents"). Holds `interface{display_name,short_description,default_prompt,...}`, `dependencies{tools[]}`, `policy{allow_implicit_invocation, products[]}` (loader.rs:72-96).
- Only `dev` gets `policy.allow_implicit_invocation: true` (024.3 §3). All others on-demand (description-triggered).
- omo exemplar: `description` = "MUST USE for ... Triggers: '...'" (routing driver). short-description optional but codexclaw includes it. Do NOT rely on `search_terms` (not in codex Interface).

## Conversion recipe (lock on dev-debugging first, 024.1 anchor)
For each skill:
1. Copy the WHOLE source skill dir from `/Users/jun/.cli-jaw-3459/skills/<name>/` — SKILL.md + references/ AND any `scripts/`, `assets/`, `examples/`, or other asset dirs the SKILL.md links to (audit blocker #2: dev-testing→scripts/examples, dev-scaffolding→assets/scripts, dev-frontend→scripts/). Unported asset dirs = dangling links. Only drop dirs that are purely cli-jaw-runtime.
2. Frontmatter: keep `name`; rewrite `description` to trigger-rich "MUST USE for ... Triggers: ..."; drop `keywords`/`triggers`/`metadata.openclaw`; add `metadata.short-description`.
3. Body rewrites (024.3 §1 conflict classes):
   - C2/C9: remove "Always injected by orchestrator" / "Injected when role=X" → routing-by-surface language.
   - C3/C4: `cli-jaw orchestrate X` → codexclaw `pabcd` flow; `cli-jaw dispatch --agent` → `spawn_agent({agent_type})`; employee/Boss → subagent/main agent.
   - C5/C8/C10: drop `devlog/`, `structure/`, `verify-counts.sh`, decade-numbering hard rule, `/Users/jun/.cli-jaw-*` paths, `cli-jaw project` → "resolve repo root via pwd/AGENTS.md". Keep universal "read docs first / don't scaffold silently".
   - C6: keep C0-C5 work classifier (universal); strip `task_tags` dispatch plumbing.
   - C7: replace `cli-jaw bgtask` paragraph → "spawn background subagent + wait_agent poll; local tests stay blocking".
4. Add `agents/openai.yaml` (policy.allow_implicit_invocation per §3; interface.display_name + short_description).
5. Verify per skill with the PRECISE gate (audit blocker #1 — bare `dispatch`/`orchestrate` false-positives on framework APIs like svelte `createEventDispatcher`, kotlin `Dispatchers.IO`, and the word "orchestrated"). Use anchored patterns ONLY:
   `rg -n "cli-jaw|cli-jaw orchestrate|orchestrate [IPABCD]\b|dispatch --agent|bgtask|verify-counts|/Users/jun/\.cli-jaw|jaw employee|--agent \"" skills/<name>/` = EMPTY.
   Do NOT match bare `dispatch` or bare `orchestrate`. Then check: frontmatter within length limits; description has triggers; no dangling asset links.

## Execution order (024.3 §5.1) + parallelization
- Phase 4a (recipe lock, SERIAL, me): convert `dev-debugging` end-to-end, run the verify grep, confirm the recipe. This is the template every other conversion copies.
- Phase 4b (BULK, parallel subagents, disjoint write scopes): once recipe is locked, dispatch subagents to convert the remaining skills in disjoint groups so write sets never overlap:
  - group 1: dev-testing, dev-security, dev-code-reviewer
  - group 2: dev-architecture, dev-data, dev-uiux-design
  - group 3: dev-frontend, dev-backend, dev-devops
  - group 4: dev-scaffolding (HIGH conflict, alone)
  Each subagent owns ONLY its skills/<name>/ dirs; given the locked dev-debugging as the worked example + the 024.3 per-skill KEEP/STRIP/REWRITE list.
- Phase 4c (HUB, SERIAL, me): convert `dev` (references all others) + merge dev-pabcd discipline into `pabcd/SKILL.md` (024.3 §4: add Depth-by-Class, work-phase terminology, anti-skip, delegation model; align with hook.ts PHASE_DIRECTIVES).

## Accept criteria (Pass 4 done = C/D)
- 13 skills present under `plugins/codexclaw/skills/` (12 dev-* + pabcd; dev-pabcd NOT a separate skill).
- Repo-wide PRECISE gate (consistent with recipe step 5; no bare dispatch/orchestrate): `rg -n "cli-jaw|cli-jaw orchestrate|orchestrate [IPABCD]\b|dispatch --agent|bgtask|verify-counts|/Users/jun/\.cli-jaw|jaw employee" plugins/codexclaw/skills/` = EMPTY (zero cli-jaw runtime leakage; legitimate framework APIs like createEventDispatcher/Dispatchers.IO are NOT flagged).
- Asset dirs (scripts/assets/examples) referenced by any converted SKILL.md are ported (no dangling internal links).
- Every SKILL.md: valid frontmatter (name ≤64, description ≤1024 with triggers, optional short-description ≤1024); `dev` has allow_implicit_invocation:true, others absent/false.
- `pabcd/SKILL.md` includes the folded dev-pabcd discipline (classifier depth, anti-skip, subagent delegation) and references no cli-jaw commands.
- node:test still 52/52 (no .ts touched — regression guard).
- skills/README.md migration stub refreshed to reflect the converted skill set (unowned-cleanup note from audit).

## A-phase audit targets (parallel subagent)
- Spot-check 3 converted skills (debugging, frontend=high-conflict, dev=hub) for residual cli-jaw-isms + frontmatter validity vs loader.rs.
- Confirm `pabcd` merge didn't drop the existing Codex-native content and aligns with hook.ts directives.
- Confirm the PRECISE grep gate is empty repo-wide AND that no legitimate framework API (createEventDispatcher, Dispatchers.IO) was wrongly stripped.

## A-phase audit OUTCOME (subagent Descartes — loader.rs/source grounded)
VERDICT: FAIL → 2 blockers found + FIXED in this revision; frontmatter/parallelization/merge all confirmed PASS.
1. Bare `dispatch`/`orchestrate` grep gate false-positives on framework APIs (svelte createEventDispatcher, kotlin Dispatchers.IO, "orchestrated") + inconsistent with accept gate. FIX: both gates now use anchored patterns (`dispatch --agent`, `orchestrate [IPABCD]`, `cli-jaw orchestrate`), never bare tokens.
2. Recipe copied only SKILL.md + references/, omitting `scripts/`/`assets/`/`examples/` dirs that dev-testing/dev-scaffolding/dev-frontend SKILL.md link to → dangling links. FIX: step 1 now copies the whole skill dir incl. asset dirs; accept criteria adds a no-dangling-links check.
PASS confirmed: loader.rs frontmatter claims correct (38-52,109-110,90-92,112-114); all 13 sources exist + dev-pabcd folds into pabcd; groups 1-4 disjoint write scopes (no race); dev-last safe (name-only cross-refs).
