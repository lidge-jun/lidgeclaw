# L5.1 (Decade 051) -- Role File Contract

Status: DONE
Cluster: 1 - Phase: 1 - Shorthand: cxc
Source-of-record: 025_subagent_as_employee.md; 025.1_pass5_P_plan.md; STATUS.md

## Goal (one slice)
Define the three codexclaw subagent role source files in a spawn-valid TOML shape.

## Why now / dependencies
L5 depends on L4 because role prompts reference the dev skill names. Before inline injection can be
documented, each role needs a stable file contract.

## Scope (decision-complete)
- Files added/edited:
  - `plugins/codexclaw/agents/explorer.toml`
  - `plugins/codexclaw/agents/reviewer.toml`
  - `plugins/codexclaw/agents/executor.toml`
- Required fields:
  - `name`
  - `description`
  - `nickname_candidates`
  - `model = "default"`
  - `developer_instructions`
- Role files shipped:
  - `explorer`
  - `reviewer`
  - `executor`
- Phase 1 model policy:
  - all roles inherit the parent model through `model = "default"`.
  - per-role model override is deferred to Phase 2.
- Must-NOT-Have:
  - No invalid `read_only` key.
  - No non-default model in Phase 1.
  - No missing developer instruction body.

## IPABCD micro-cycle
- I (if interview-bearing): not interview-bearing.
- P: planned three roles only, matching the MVP subagent surfaces.
- A: Pass 5 audit confirmed `read_only` is not a valid role file field.
- B: enriched all three TOMLs into the common omo-like shape.
- C: light gate verified required fields and unchanged pabcd-state tests stayed 52/52.
- D: done = each role file is parseable as a prompt source and default-model config.

## Acceptance (1-3 testable criteria)
- All three role TOMLs contain the five required fields.
- No role TOML contains `read_only`.
- Every role uses `model = "default"`.

## QA channel (node:test path / CLI stdout / tmux / data dump)
- Pass 5 light gate: three TOMLs plus README verified for required fields.
- `npm test` was unchanged for code paths: pabcd-state 52/52 at Pass 5 and root 73/73 final.

## Commit unit (one atomic conventional commit)
Role file enrichment landed in Pass 5 B commit 5bcd20c.

## Blocked-on (jun decision id, if any)
None.

## References (codex-rs paths, omo skills, ouroboros, source-of-record docs)
- `plugins/codexclaw/agents/explorer.toml`
- `plugins/codexclaw/agents/reviewer.toml`
- `plugins/codexclaw/agents/executor.toml`
- `devlog/_plan/260629_codexclaw_mvp/025_subagent_as_employee.md`
- `devlog/_plan/260629_codexclaw_mvp/025.1_pass5_P_plan.md`
