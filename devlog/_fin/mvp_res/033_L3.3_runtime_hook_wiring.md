# L3.3 (Decade 033) -- Runtime Hook Wiring

Status: DONE
Cluster: 1 - Phase: 1 - Shorthand: cxc
Source-of-record: 023_goal_convention_port.md; 160_pass1_7_completeness_audit.md; STATUS.md

## Goal (one slice)
Make the implemented goal-budget guard reachable at Codex runtime through the plugin manifest.

## Why now / dependencies
L3.1 and L3.2 made the component correct but the completeness audit later found it was not wired into
the plugin manifest. This sub-pass records the as-built B1 wiring-gap correction.

## Scope (decision-complete)
- Files added/edited:
  - `plugins/codexclaw/hooks/pre-tool-use-guarding-goal-budget.json`
  - `plugins/codexclaw/.codex-plugin/plugin.json`
  - `plugins/codexclaw/components/pabcd-state/src/cli.ts`
  - `plugins/codexclaw/components/pabcd-state/dist/cli.js`
- Hook registration:
  - plugin manifest `hooks` includes `./hooks/pre-tool-use-guarding-goal-budget.json`.
  - hook event is `PreToolUse`.
  - command is `node "${PLUGIN_ROOT}/components/pabcd-state/dist/cli.js" hook pre-tool-use`.
  - timeout is 15 seconds.
  - status message is `(codexclaw) Guarding goal budget`.
- Runtime proof:
  - budgeted `create_goal` now reaches `permissionDecision:"deny"`.
  - source-of-record marks B1 resolved in commit ba20b64.
- Must-NOT-Have:
  - No dead source-only guard.
  - No uncompiled `.ts` command path in hook JSON.
  - No manifest edit outside codexclaw's own plugin entry.

## IPABCD micro-cycle
- I (if interview-bearing): not interview-bearing.
- P: completeness audit identified B1 as "implemented but unreachable."
- A: fix scope was narrowed to hook JSON plus manifest registration.
- B: added the PreToolUse hook JSON and registered it as the fourth hook in plugin.json.
- C: e2e evidence: budgeted `create_goal` produced `permissionDecision:"deny"`; root tests stayed 73/73.
- D: done = L3 is shipped, not merely component-done.

## Acceptance (1-3 testable criteria)
- `plugin.json` lists `pre-tool-use-guarding-goal-budget.json`.
- The hook JSON command points at compiled `pabcd-state/dist/cli.js`.
- A budgeted `create_goal` is denied through the runtime hook path.

## QA channel (node:test path / CLI stdout / tmux / data dump)
- Unit coverage: `goal-gate.test.ts` 9 tests.
- Final Phase 1 regression after wiring: root `npm test` 73/73.
- Wiring source-of-record: 160 audit B1 and STATUS wiring-gap fix notes.

## Commit unit (one atomic conventional commit)
Runtime hook wiring landed in ba20b64.

## Blocked-on (jun decision id, if any)
None.

## References (codex-rs paths, omo skills, ouroboros, source-of-record docs)
- `plugins/codexclaw/hooks/pre-tool-use-guarding-goal-budget.json`
- `plugins/codexclaw/.codex-plugin/plugin.json`
- `devlog/_plan/260629_codexclaw_mvp/160_pass1_7_completeness_audit.md`
- `devlog/_plan/260629_codexclaw_mvp/STATUS.md`
