# L1.2 (Decade 012) -- FSM Gates

Status: DONE
Cluster: 1 - Phase: 1 - Shorthand: cxc
Source-of-record: 018_pass1_P_plan.md; 022.1_pabcd_state_files.md; STATUS.md

## Goal (one slice)
Ship the pure IPABCD transition predicates and helper gates without touching files or hook IO.

## Why now / dependencies
This sits after the state schema and before any hook logic. L2 can inject phase directives only after
the phase vocabulary is stable, and later loops can test gates without depending on Codex runtime IO.

## Scope (decision-complete)
- Files added/edited:
  - `plugins/codexclaw/components/pabcd-state/src/fsm.ts`
  - `plugins/codexclaw/components/pabcd-state/test/fsm.test.ts`
- Public functions:
  - `canEnter(to, state)`
  - `nextPhase(state)`
  - `isAuditGateOpen(state)`
  - `isBuildGateOpen(state)`
  - `isDone(state)`
- Gate semantics:
  - `I` is always enterable.
  - `P` requires `state.flags.interview`.
  - `A` is always enterable once selected by the caller.
  - `B` requires `state.flags.auditPassed`.
  - `C` is always enterable once selected by the caller.
  - `D` requires `state.flags.checkPassed`.
  - `nextPhase` follows canonical `I,P,A,B,C,D` and returns null after `D`.
- Must-NOT-Have:
  - No ledger append.
  - No state writes.
  - No automatic runtime advance from `Stop`.

## IPABCD micro-cycle
- I (if interview-bearing): gate data only; no request_user_input loop in L1.
- P: keep FSM pure and import `PHASES` from `state.ts` for one-way dependency.
- A: audit confirmed the cycle fix and kept runtime transition side effects out of this file.
- B: implemented `ORDER`, `canEnter`, `nextPhase`, and helper predicates.
- C: `fsm.test.ts` has seven tests for the ordered phases and all closed gates.
- D: done = transition rules are deterministic, side-effect-free, and independently testable.

## Acceptance (1-3 testable criteria)
- `canEnter("P", state)` is false until `flags.interview` is true.
- `canEnter("B", state)` is false until `flags.auditPassed` is true.
- `canEnter("D", state)` is false until `flags.checkPassed` is true.

## QA channel (node:test path / CLI stdout / tmux / data dump)
- `plugins/codexclaw/components/pabcd-state/test/fsm.test.ts`: 7 tests.
- Test names include `I->P blocked without interview flag`, `B blocked without auditPassed`,
  `D blocked without checkPassed`, `nextPhase follows ORDER`, and `gate helpers`.
- Pass 1 gate: 16/16; final Phase 1 regression: root `npm test` 73/73.

## Commit unit (one atomic conventional commit)
FSM gates landed in Pass 1 B commit b1d3519 and were hardened in 13bf8d1.

## Blocked-on (jun decision id, if any)
None.

## References (codex-rs paths, omo skills, ouroboros, source-of-record docs)
- `plugins/codexclaw/components/pabcd-state/src/fsm.ts`
- `plugins/codexclaw/components/pabcd-state/test/fsm.test.ts`
- `devlog/_plan/260629_codexclaw_mvp/018_pass1_P_plan.md`
- `devlog/_plan/260629_codexclaw_mvp/022.1_pabcd_state_files.md`
- Runtime auto-advance is documented as deferred in L2/L7 source records.
