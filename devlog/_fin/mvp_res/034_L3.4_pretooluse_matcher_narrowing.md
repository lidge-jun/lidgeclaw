# L3.4 (Decade 034 slot) -- Narrow PreToolUse matcher to ^create_goal$

Status: DONE (007 R-10)
Cluster: 1 (L3 hardening) - Phase: 1 - Shorthand: cxc
Source-of-record: 000_research_src/007_impl_reality_findings.md (R-10);
021.1_codex_rs_skill_mechanism.md:72 (matcher syntax); omo ulw-loop/src/codex-hook.ts

## Goal (one slice)
Add `"matcher": "^create_goal$"` to the PreToolUse hook entry so the goal-budget guard fires ONLY
on `create_goal`, matching omo parity. Shipped manifest had no matcher -> the hook subprocess spawned
on every tool call (the goal-gate.ts internal early-return kept it correct, but wasteful + broad).

## Why now / dependencies
Part of the L1-L7 doc<->impl reconciliation. R-10 is an L3 (goal-gate) hardening item. Narrowing the
matcher reduces per-tool subprocess overhead and documents intent at the registration surface (the
hook should self-describe its scope, not rely solely on internal filtering).

## Scope (decision-complete)
- File edited: `plugins/codexclaw/hooks/pre-tool-use-guarding-goal-budget.json`
  - add `"matcher": "^create_goal$"` as a sibling of the inner `hooks` array (codex-rs schema, 021.1:72)
- Verification: add a manifest test asserting the PreToolUse entry carries the create_goal matcher.
- Must-NOT-Have:
  - No change to goal-gate.ts logic (the internal create_goal early-return stays as defense-in-depth).
  - request_user_input deny is L11 scope, NOT folded here (kept atomic).

## IPABCD micro-cycle
- P: this doc.
- A: matcher syntax confirmed against codex-rs research (021.1:72) + omo example; sibling-of-hooks shape.
- B: edit manifest + add manifest assertion test.
- C: npm test green; manifest still valid JSON.
- D: hook self-describes its create_goal scope; matcher asserted by test.

## Acceptance (testable)
- The PreToolUse hook entry in the manifest has `matcher === "^create_goal$"`.
- npm test stays green.

## QA channel
- `plugins/codexclaw/test/*.test.mjs` manifest assertion; root `npm test`.

## Commit unit
One atomic commit: manifest matcher + assertion test.

## Blocked-on
None (R-10 resolved in 007).

## References
- `devlog/_plan/mvp_res/000_research_src/007_impl_reality_findings.md` (R-10)
- `devlog/_plan/mvp_res/000_research_src/021.1_codex_rs_skill_mechanism.md`
- `devlog/.lazycodex/plugins/omo/skills/`
- `plugins/codexclaw/hooks/pre-tool-use-guarding-goal-budget.json`
- `plugins/codexclaw/test/manifest-policy.test.mjs`
