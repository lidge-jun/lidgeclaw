# L6.2 (Decade 062) -- Activation Manifest and Backup

Status: DONE
Cluster: 1 - Phase: 1 - Shorthand: cxc
Source-of-record: 027_config_untouched_guard.md; 028.1_install_activation.md; 028.2_pass6_P_plan.md

## Goal (one slice)
Enable only missing cxc-declared Codex feature flags and record exactly what changed.

## Why now / dependencies
L6.1 provides the declared feature set and current-state parser. This sub-pass performs activation
through the official Codex CLI and creates the manifest that L6.3 uses for safe revert.

## Scope (decision-complete)
- Files added/edited:
  - `plugins/codexclaw/components/config-guard/src/activate.ts`
  - `plugins/codexclaw/components/config-guard/src/features.ts`
  - `plugins/codexclaw/components/config-guard/test/activate.test.ts`
- Constants and types:
  - `INSTALL_MANIFEST = ".codexclaw-install.json"`
  - `FlagRecord`
  - `InstallManifest`
  - `ActivateDeps`
- Functions shipped:
  - `manifestPath(codexHome)`
  - `activate(deps)`
- Activation behavior:
  - reads current flag state through `codex features list`.
  - backs up `config.toml` before mutation.
  - enables only declared flags that are not already true.
  - records `priorEnabled`, `enabledByCodexclaw`, `enableFailed`, and `postActivateHash`.
  - tolerates soft failure for `default_mode_request_user_input`.
- Must-NOT-Have:
  - No hand-written TOML mutation by codexclaw.
  - No mutation of flags already true.
  - No tests against real `~/.codex`.

## IPABCD micro-cycle
- I (if interview-bearing): not interview-bearing.
- P: pivoted from "config untouched" to controlled feature activation via official CLI.
- A: audit required injected runner/codexHome, backup, manifest, and soft-feature behavior.
- B: implemented activation around `codex features enable`.
- C: activate tests prove only missing flags enable, manifest writes, backup writes, and idempotent re-enable.
- D: done = activation is attributable and reversible.

## Acceptance (1-3 testable criteria)
- Pre-existing true flags are recorded as prior, not as cxc-enabled.
- Missing declared flags are enabled and written to `.codexclaw-install.json`.
- A second activation issues no redundant enable calls.

## QA channel (node:test path / CLI stdout / tmux / data dump)
- `plugins/codexclaw/components/config-guard/test/activate.test.ts`: 7 tests total.
- Relevant tests: enables only not-already-true flags, writes manifest and backup,
  idempotent re-enable, soft flag failure does not abort.
- Config-guard final count: 15/15; root `npm test` 73/73.

## Commit unit (one atomic conventional commit)
Activation manifest and backup landed in Pass 6 B commit 4a1c08f.

## Blocked-on (jun decision id, if any)
None.

## References (codex-rs paths, omo skills, ouroboros, source-of-record docs)
- `plugins/codexclaw/components/config-guard/src/activate.ts`
- `plugins/codexclaw/components/config-guard/test/activate.test.ts`
- `devlog/_plan/260629_codexclaw_mvp/027_config_untouched_guard.md`
- `devlog/_plan/260629_codexclaw_mvp/028.1_install_activation.md`
