# L1.4 (Decade 014) -- IDLE/complete closed state + structural attest enforcement

Status: DONE (007 R-2 + R-4)
Cluster: 1 (L1 hardening) - Phase: 1 - Shorthand: cxc
Source-of-record: 000_research_src/007_impl_reality_findings.md (R-2, R-4); cli-jaw
state-machine.ts:646 + attestation.ts:36/107; 012_L1.2_fsm_gates.md (supersedes flag-only gates)

## Goal (one slice)
Close two foundational FSM principle gaps the 007 cross-reference (cli-jaw/jawcode/omo) found in the
shipped L1 engine, by editing real code in `pabcd-state`:
- R-4: add an explicit **IDLE** closed/rest phase so a cycle actually closes (`D -> IDLE`) before the
  next work-phase `P` can open. Shipped code had `phase` default `I` and `nextPhase(D)=null` (never
  returns to a rest state), contradicting the "D returns to IDLE to bound scope drift" design.
- R-2: make the audit/check evidence gate **structural**, not decorative. Shipped `fsm.ts` exposes
  `auditPassed`/`checkPassed` flags that NO code path ever sets, and `A`/`C` entry is unconditionally
  open, so the evidence gate survived only as prompt prose. Port cli-jaw's `GATED_TRANSITIONS` +
  attestation contract: `A->B` and `C->D` require a valid attestation object, and the gate flags flip
  to true ONLY inside `transition()` when that attestation validates.

## Why now / dependencies
L8+ (interview freeze -> goal handoff) trusts the FSM to actually close cycles and to gate forward
motion on evidence. If `D` never closes to IDLE and the gate flags are unsettable, every later loop
inherits a decorative state machine. This must land before Cluster 1 build work.

## Scope (decision-complete)
- Files edited:
  - `plugins/codexclaw/components/pabcd-state/src/state.ts` (add IDLE to phase vocabulary; default
    state -> IDLE; validation accepts IDLE; `Attestation` type)
  - `plugins/codexclaw/components/pabcd-state/src/fsm.ts` (IDLE handling in `canEnter`/`nextPhase`;
    new `transition(state, to, attest?)` enforcing gated attest)
  - `plugins/codexclaw/components/pabcd-state/src/hook.ts` (IDLE-safe directive/label lookups)
- Files added:
  - `plugins/codexclaw/components/pabcd-state/src/attest.ts` (`validateAttest`, `GATED_TRANSITIONS`)
  - `plugins/codexclaw/components/pabcd-state/test/attest.test.ts`
- Public additions:
  - `IDLE` phase; `WORK_PHASES` (I..D) kept distinct from `ALL_PHASES` (IDLE + I..D)
  - `Attestation { from; to; did; checkOutput?; exitCode? }`
  - `validateAttest(from, to, attest)`, `GATED_TRANSITIONS`
  - `transition(state, to, attest?): { ok; state?; reason? }`
- Gate semantics (port from cli-jaw):
  - `A->B` is gated: requires an attestation with non-empty `did`. On success `flags.auditPassed=true`.
  - `C->D` is gated: requires `did` + non-empty `checkOutput` + `exitCode===0`. On success
    `flags.checkPassed=true`.
  - Forward gated transition WITHOUT a valid attest is rejected (`ok:false`); no flag flips, no
    phase change (fail-closed, cli-jaw 409 parity).
  - `D->IDLE` closes the cycle: phase=IDLE, `orchestrationActive=false`, gate flags reset.
  - `IDLE->P` is valid without an interview flag (interview is optional); `I->P` still requires
    `flags.interview` (preserves L1.2 contract).
- Must-NOT-Have:
  - No change to hook stdin/stdout envelope shape.
  - No server dependency (`transition` is pure; callers persist via `writeState`).
  - No auto-advance from passive `Stop`.

## IPABCD micro-cycle
- P: this doc — diff-level scope + accept criteria, grounded in shipped code + 007 + cli-jaw refs.
- A: 007 cross-reference IS the adversarial audit (cli-jaw/jawcode/omo three-way); verdict = R-2/R-4
  are real principle losses. Re-verified live: `auditPassed`/`checkPassed` have zero writers in src.
- B: implement attest.ts + transition() + IDLE vocabulary in small commits.
- C: `npm test` green (new attest + IDLE tests), build idempotent, no placeholder markers.
- D: gate flags are structurally unsettable without evidence; cycle closes to IDLE.

## Acceptance (testable)
- `transition(stateInA, "B")` with no attest -> `ok:false`; with `{from:"A",to:"B",did:"..."}` ->
  `ok:true` and `flags.auditPassed===true`.
- `transition(stateInC, "D", {from:"C",to:"D",did:"ran",exitCode:1,checkOutput:"x"})` -> `ok:false`;
  with `exitCode:0` + non-empty `checkOutput` -> `ok:true` and `flags.checkPassed===true`.
- `nextPhase(stateInD)` returns `"IDLE"`; `defaultState(...).phase === "IDLE"`.
- `canEnter("P", {phase:"IDLE"})` is ok without interview; `canEnter("P", {phase:"I", interview:false})`
  stays blocked.

## QA channel
- `node --test` on pabcd-state + new `attest.test.ts`; root `npm test` regression must stay green.

## Commit unit
Small atomic conventional commits: (1) IDLE vocabulary, (2) attest module + transition gate, (3) tests.

## Blocked-on
None (R-2/R-4 decisions resolved in 007).

## References
- `devlog/_plan/mvp_res/000_research_src/007_impl_reality_findings.md` (R-2, R-4)
- cli-jaw `state-machine.ts:646`
- cli-jaw `attestation.ts:36/107`
- `devlog/_plan/mvp_res/012_L1.2_fsm_gates.md`
- `plugins/codexclaw/components/pabcd-state/src/state.ts`
- `plugins/codexclaw/components/pabcd-state/src/fsm.ts`
- `plugins/codexclaw/components/pabcd-state/src/attest.ts`
