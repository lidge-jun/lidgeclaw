# 015.1 — cli-jaw → codexclaw Porting Map

Date: 2026-06-29
Status: CONFIRMED DIRECTION (supersedes "strip" framing in 024.2 C3/C4)
Decision (jun): Do NOT strip cli-jaw mechanisms — PORT them to codex equivalents.
- cli-jaw commands → codex-native equivalents.
- cli-jaw "worker"/"employee" → codex subagent (agent role).
- Universal discipline → always included (it is the point, not optional).
- codexclaw skills are ORIGINAL rewrites, fully independent from cli-jaw; IPABCD skill is
  rewritten to match codexclaw's OWN FSM implementation.

## Sources (MUST cite when building)
- codex agent roles: `/Users/jun/Developer/codex/121_openai-codex/codex-rs/core/src/agent/role.rs`
  (+ `registry.rs`, `builtins/{explorer,awaiter}.toml`, `config/agent_roles`).
- codex skills: `core-skills/src/loader.rs` (see 021.1).
- exemplar: `devlog/.lazycodex/plugins/omo` (ulw-loop SKILL.md spawn table; ultrawork agents/*.toml).

## Ground truth: codex subagent = "agent role"
- A role file is loaded with the SAME machinery as `config.toml` (role.rs:1-7).
- Role chosen at spawn via `agent_type`; the multi-agent tool handler owns orchestration.
- Role layer = session-flag precedence: overrides persisted config, BUT caller's
  `model_provider`/`service_tier` stay sticky unless the role sets them → per-role model override
  is supported (basis for Phase 2 multi-model).
- Parser: `AgentRoleConfig` + `parse_agent_role_file_contents`. Built-ins: explorer, awaiter.
- Spawn tools (from omo usage): `multi_agent_v1.spawn_agent / wait_agent / close_agent`,
  `fork_context: false` default.

## Command porting table (cli-jaw verbs found in dev skills)
Measured: `cli-jaw orchestrate` 23, `cli-jaw dispatch` 4, `orchestrate reset` 3,
`bgtask add` 2, `runtime`/`project add|clear`/`goal update` 1 each.

| cli-jaw mechanism | codex-native port | Notes / source |
|-------------------|-------------------|----------------|
| `cli-jaw orchestrate I/P/A/B/C/D` | codexclaw IPABCD FSM (`.codexclaw/sessions/<sessionId>.json`, per-session — see 016) driven by hooks + `pabcd` skill | 022/022.1; no server |
| `cli-jaw orchestrate reset` | codexclaw `pabcd` reset (clear state file) | 022 |
| `cli-jaw dispatch --agent X` | `multi_agent_v1.spawn_agent({agent_type:"X", message:"TASK:..."})` | role.rs; 025 |
| cli-jaw "employee"/"worker" | codex agent role (.toml) spawned as subagent | role.rs builtins |
| `cli-jaw goal set/update/done` | codex `create_goal`/`update_goal` (+ omo-style gate) | 023; create_goal native |
| `cli-jaw bgtask add` (server-owned wait) | codex background subagent + `wait_agent` polling; OS scheduler for periodic (Phase 3) | 040; 025 |
| `cli-jaw project add/clear` | codex working-dir / config layer (cwd-scoped) | role.rs config layers |
| `cli-jaw runtime` | codex runtime (the harness itself) — drop the wrapper verb | n/a |

## Discipline porting (the "보편규율 당연히 포함" part)
- `dev` (common) work classifier C0-C5 + universal rules → port as an ALWAYS-INCLUDED codexclaw
  skill. Confirmed: universal discipline is mandatory, not opt-in.
- Replace cli-jaw plumbing words (boss/employee/dispatch/bgtask/devlog house rules) with the
  ported codex equivalents above — keep the DISCIPLINE, swap the MECHANISM.

## IPABCD skill stance (confirmed)
- codexclaw owns an ORIGINAL `pabcd` skill, independent of cli-jaw.
- Its wording is written to match codexclaw's FSM (022), NOT cli-jaw orchestrate commands.
- `dev-pabcd` is therefore NOT ported as a separate skill; its intent is absorbed by `pabcd` +
  this porting map.

## Impact on 024.2
- C3 (orchestrate commands) and C4 (boss/employee): reclassify from "remove" to "PORT per table above".
- C5/C6/C7: keep discipline, port mechanism; drop only cli-jaw-repo-specific filenames/paths.

## Open (next mini-A)
- Q-PORT-1: subagent role files location for codex pickup — `~/.codex/config` agent_roles vs
  plugin-provided. Verify in 025 against role.rs `resolve_role_config` + registry.rs.
- Q-PORT-2: does `create_goal`/`update_goal` exist as a tool in THIS codex build, or only via a
  plugin? Confirm before relying (023).
