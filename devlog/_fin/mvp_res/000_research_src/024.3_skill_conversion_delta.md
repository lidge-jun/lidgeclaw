# 024.3 — dev-* Skill Conversion Delta (file-by-file)

Status: RESEARCH+DESIGN (source-verified)  ·  Phase 1 / Pass 4-5
Purpose: Concrete, file-by-file delta of WHICH PARTS of each cli-jaw dev-* skill must change
to become valid codexclaw Codex skills, and HOW. Supersedes the high-level rules in 024 and
the conflict catalog in 024.2 with a per-file action plan.

Ground rules (from 015.1, restated):
- PORT, don't strip. cli-jaw mechanisms → codex-native equivalents. Universal discipline stays.
- All 13 dev skills ship as Codex **subagent ROUTER roles** (024, 024.1, 025).
- `dev/SKILL.md` = always-on universal discipline. The 12 surface skills = on-demand routers.
- codexclaw skills are ORIGINAL rewrites, fully independent of cli-jaw (port-not-copy).

Sources cited (MUST read before implementing):
- cli-jaw skills: `/Users/jun/.cli-jaw-3459/skills/dev*/SKILL.md`
- codex-rs loader: `/Users/jun/Developer/codex/121_openai-codex/codex-rs/core-skills/src/loader.rs:40-58` (SkillFrontmatter), `:110` (openai.yaml)
- codex-rs config: `/Users/jun/Developer/codex/121_openai-codex/codex-rs/config/src/skills_config.rs`
- 021.1 (frontmatter ground truth), 024.2 (conflict classes C1-C8), 015.1 (porting map), 024 (conversion rules), 024.1 (recipe anchor)
- omo exemplars: `devlog/.lazycodex/plugins/omo/skills/{debugging,frontend,ulw-plan}/SKILL.md`
- existing codexclaw: `plugins/codexclaw/skills/{README.md,pabcd/SKILL.md}`
- pabcd-state hook: `plugins/codexclaw/components/pabcd-state/src/hook.ts` (PHASE_DIRECTIVES, detectTrigger)
- jawcode port precedent: `jawcode/packages/coding-agent/src/prompts/jaw/orchestrate-{i,p}.md`

---

## 1. Conflict-Class Table

Every cli-jaw construct that appears in the dev-* skills, its codex equivalent, and the rewrite rule.

| # | cli-jaw construct (with source line) | Codex equivalent | Rewrite rule |
|---|---|---|---|
| C1 | `metadata: { "keywords": ["connector"] }` (dev/SKILL.md:6) | `metadata: { short-description: "..." }` (loader.rs:52, rename `short-description`) | Drop `keywords` entirely (not in `SkillFrontmatterMetadata`, loader.rs:50-54). Move intent text into `description` + `short-description`. |
| C2 | `"Always injected by orchestrator."` (dev/SKILL.md:3) | codex auto-routes via `description` + `policy.allow_implicit_invocation` (021.1) | Remove "injected by" framing. Rewrite `description` as trigger-rich "MUST USE for ..." line (omo style). Set `allow_implicit_invocation: true` in `openai.yaml` for `dev` only. |
| C3 | `cli-jaw orchestrate I` (dev-pabcd/SKILL.md:12) | codexclaw IPABCD FSM — `pabcd` skill + `pabcd-state` hook `detectTrigger()` (hook.ts:55-63) | Replace every `cli-jaw orchestrate X` with the codexclaw phase-directive flow. dev-pabcd is NOT a separate skill — it folds into `pabcd` (015.1). |
| C4 | `cli-jaw dispatch --agent "Backend"` (dev-pabcd/SKILL.md:Repository Root Contract) | `multi_agent_v1.spawn_agent({agent_type:"backend", message:"TASK:..."})` (015.1 porting table; 025) | Remap "employee"/"worker"/"Boss" → "subagent"/"main agent". Drop server re-invoke + bgtask wording. |
| C5 | `devlog/`, `structure/`, decade numbering "00-09 research" (dev/SKILL.md:0.5, dev-pabcd/SKILL.md:Jawdev Document Numbering) | Generic "source-of-truth docs" — read before broad changes; never scaffold silently | Keep the universal principle (read docs first, don't create structure folders without approval). Drop cli-jaw-specific filenames/paths. Decade numbering → optional guidance, not hard rule. |
| C6 | `task_tags` set "by the boss at dispatch" (dev/SKILL.md:§0.3) | Self-assessment overlays — the subagent reads its role skill by surface | Keep the C0-C5 classifier (valuable, universal). Strip `task_tags` dispatch plumbing. Replace "the boss sets task_tags" with "the routing skill description activates the overlay." |
| C7 | `cli-jaw bgtask add --cmd '...'` (dev/SKILL.md:§3 Long external waits) | codex background subagent + `wait_agent` polling; OS scheduler for periodic (015.1, 040) | Remove the bgtask/server-owns-the-wait paragraph. Replace with: spawn a background subagent for long external work, poll with `wait_agent`. Local tests/tsc stay blocking. |
| C8 | `dev-pabcd/SKILL.md`, `/Users/jun/.cli-jaw-3459/skills/...` cross-refs | codexclaw skill names (`pabcd`, `dev-backend`, etc.); never hardcode cli-jaw home path | Rewrite all cross-skill references to codexclaw names. Remove absolute `/Users/jun/.cli-jaw-*` paths. |
| C9 | `"Injected when role=frontend"` (dev/SKILL.md Companion Skills table) | codex description-triggered routing; subagent inline instructions name the router (024 B-opt2) | Rewrite the Companion Skills table as a routing table keyed by change-surface, not by cli-jaw `role=` dispatch. |
| C10 | `"cli-jaw project add/clear"` (dev-pabcd/SKILL.md:Repository Root Contract) | codex working-dir / cwd-scoped config layers (015.1) | Replace project-root-CLI wording with: resolve repo root via `pwd -P` / AGENTS.md; state it in subagent task bodies. |

---

## 2. Per-Skill Change List (ALL 13)

Classification key:
- **A** = always-on discipline (implicit invocation, loaded on every turn)
- **R** = router role (on-demand, activated by description trigger / subagent inline ref)

Each skill: KEEP (universal content) / STRIP (cli-jaw runtime) / REWRITE (command→native or →subagent role).

### 2.1 `dev` — Common Development Guidelines  [A]

**Classification:** ALWAYS-ON. This is the universal discipline entry, exactly as in cli-jaw (024).

**KEEP:**
- §0.0 Work Classifier C0-C5 (dev/SKILL.md:§0.0) — the table and tie-break rules are universal.
- §0.1 Patch Fast-Path (C0/C1) — scope guidance, universal.
- §0.2 Rule Classes (STRICT/DEFAULT/HEURISTIC/STYLE_SAMPLE/ESCALATE) — universal.
- §1 Modular Development limits table (400 LOC, 50-line functions, etc.) — universal.
- §1.5 Pre-Write Codebase Search Obligation — universal.
- §2 Systematic Debugging red-flag table — universal (delegates to dev-debugging).
- §3 Verification Before Completion gate — universal. **BUT** strip the bgtask paragraph (see STRIP).
- §4 Change Documentation format — universal.
- §5 Safety Rules — universal.
- §6 Code Quality Signals — universal.
- §7 Type Safety & Static Analysis — universal.
- §8 Token Budget Awareness — universal (tiered reference loading).

**STRIP:**
- §0.3 `task_tags` dispatch plumbing — "The boss sets task_tags at dispatch" (dev/SKILL.md:§0.3, C6).
- §0.4 "isolated employee", "mutable employee (`--mutable`)", "goal mode (self-advancing gates)" cli-jaw-specific mode list — keep the mode concept, strip the cli-jaw mode names.
- §3 "Long external waits (cli-jaw runtime)" paragraph — `cli-jaw bgtask add` (dev/SKILL.md:§3, C7).
- Companion Skills table column "Injected When" with `role=frontend` etc. (C9).
- "Documentation Verification (Context7)" — keep Context7 usage but this is MCP-dependent; make it conditional ("if Context7 MCP is available").
- §0.5 "Devlog phase documents: decade-range Jawdev numbering" hard rule (C5) — soften to optional.
- Skill Ownership Map stub references to cli-jaw paths — rewrite to codexclaw names.

**REWRITE:**
- Frontmatter: `metadata: { "keywords": ["connector","dashboard"] }` → `metadata: { short-description: "Universal dev discipline: work classifier, modular limits, verification gate, safety rules." }` (C1).
- Description: `"Common development guidelines... Always injected by orchestrator."` → `"MUST USE for every coding task — classifies work depth (C0-C5), enforces modular limits, pre-write search, verification-before-completion, and safety rules. Always-on discipline that routes to surface-specific dev-* routers by change surface."` (C2).
- §0.3 Methodology Overlays: replace "task_tags set at dispatch" with "overlays activate when the routing skill's description matches the work surface." The overlay→skill table itself stays (tdd→dev-testing, security→dev-security, etc.) but the activation mechanism changes.
- §0.4 Workflow Modes: rewrite mode list to codex terms — "ordinary chat", "PABCD mode (pabcd skill)", "goal mode (create_goal)", "subagent (scoped writes via spawn_agent)", "read-only review".
- §3 Long external waits: replace bgtask paragraph with: "For long-running external verification (CI, deploy), spawn a background subagent and poll with `wait_agent`. Local tsc/tests stay blocking." (C7).
- Companion Skills table: rewrite "Injected When" as "Routes When (surface)" — `role=frontend` → "UI/frontend work", etc. (C9).
- Add `agents/openai.yaml` with `policy.allow_implicit_invocation: true` (always-on).

**Example conversion (frontmatter):**

cli-jaw original (dev/SKILL.md:1-7):
```yaml
---
name: dev
description: "Common development guidelines for all orchestrated sub-agents. Enforces modular development, systematic debugging, verification-before-completion, change logging, and code quality standards. Always injected by orchestrator."
metadata:
  { "keywords": ["connector", "dashboard"] }
---
```

codexclaw converted:
```yaml
---
name: dev
description: "MUST USE for every coding task — classifies work depth (C0-C5), enforces modular limits, pre-write search, verification-before-completion, and safety rules. Always-on discipline that routes to surface-specific dev-* routers by change surface. Triggers: any code change, refactor, bug fix, feature, test, review, scaffolding."
metadata:
  short-description: "Universal dev discipline: work classifier, modular limits, verification gate."
---
```

**Example conversion (§3 long waits):**

cli-jaw original:
> **Long external waits (cli-jaw runtime):** when a verification gate depends on a long-running external process ... do not block the turn polling it. Register a server-owned task and end the turn: `cli-jaw bgtask add --cmd '["gh","run","watch","123","--exit-status"]' --prompt "CI done: {{result}} — verify and report"`

codexclaw converted:
> **Long external verification:** when a gate depends on a long-running external process (CI run, deploy, remote build), spawn a background subagent (`multi_agent_v1.spawn_agent`) and poll with short `multi_agent_v1.wait_agent` cycles. Local commands (tsc, tests, builds that finish in minutes) stay blocking — background is for genuinely long external work.

---

### 2.2 `dev-architecture` — Module Boundaries  [R]

**Classification:** ROUTER. Activated when work touches module structure, boundaries, circular deps.

**KEEP:** Entire body is nearly universal — §1 Module Boundaries (layered table, split signals, banned patterns, SSOT, deep modules), §2 Circular Dependency detection commands + fix guidance, §3 Implicit Coupling Taxonomy (8 types), §4 Boundary-Only Defensive Programming (validation location matrix), §5 Barrel/Re-export Discipline, §6 Review Integration. The `references/` split (circular-dependencies.md, coupling-taxonomy.md, barrel-discipline.md) stays as progressive disclosure.

**STRIP:**
- "Always read `dev/SKILL.md` first" → keep but reword to "read the `dev` skill (universal discipline) first."
- Cross-skill references to cli-jaw paths: "See `dev-backend/references/core/observability.md`" → keep as codexclaw relative ref `dev-backend/references/core/observability.md`.
- `metadata: { "keywords": [...] }` (C1).

**REWRITE:**
- Frontmatter description → trigger-rich: `"MUST USE for module boundary work, circular dependency detection/prevention, coupling taxonomy, barrel/re-export discipline, and boundary-only defensive programming. Triggers: circular import, module split, layer violation, utils.ts growth, barrel file, re-export, coupling review, dependency direction, madge, depcruise."`
- Add `agents/openai.yaml` with `policy.allow_implicit_invocation: false` (on-demand router).
- §6 CI Recommendations table — keep the tool commands but they are advisory, not cli-jaw runtime.

---

### 2.3 `dev-backend` — API/Server/DB  [R]

**Classification:** ROUTER. Activated for API design, server architecture, DB optimization, middleware.

**KEEP:** §1-§8 API design patterns, error handling (Result pattern / neverthrow), middleware, DB optimization, caching, observability (OTel), async task queues, long-lived connections, process isolation. The `references/` tree (core/caching.md, core/observability.md, etc.) stays.

**STRIP:**
- "Injected when role=backend" framing (C9).
- Any `cli-jaw dispatch` / employee references in cross-skill stubs.
- `metadata: { "keywords": [...] }`.

**REWRITE:**
- Description → `"MUST USE for backend, API, server, or database work — API design, clean architecture, database optimization, error handling, middleware, observability, async task queues, long-lived connections. Triggers: API endpoint, REST, GraphQL, schema, migration, query optimization, middleware, OTel, caching, Result pattern, neverthrow."`
- §5 envelope/OTel production-surface scoping → keep the "production surface" definition but port it from cli-jaw's to a generic one (the definition already says "deployed for real users" — universal).
- Cross-refs: "See `dev-architecture §4`" → keep as codexclaw relative name.

---

### 2.4 `dev-code-reviewer` — Code Review  [R]

**Classification:** ROUTER. Activated for review requests or C3/C4 work.

**KEEP:** Review process, quality thresholds, antipattern detection catalog (§3), giving/receiving feedback, review decision matrix. The review checklist is universal.

**STRIP:**
- "Available to any agent regardless of role" → reword to "any subagent or the main agent."
- Minor employee/dispatch references (C4).
- `metadata: { "keywords": [...] }`.

**REWRITE:**
- Description → `"MUST USE for code review — review process, quality thresholds, antipattern detection, giving and receiving feedback. Triggers: review this, code review, PR review, check my code, review before merge, antipattern, review checklist."`
- §3 antipattern table cross-refs to `dev` §6 → keep as codexclaw `dev` skill reference.
- `agents/openai.yaml` with `allow_implicit_invocation: false`.

---

### 2.5 `dev-data` — Data/Pipelines  [R]

**Classification:** ROUTER. Activated for ETL/ELT, data quality, SQL optimization, analysis.

**KEEP:** Data pipeline patterns, ETL/ELT design, data quality validation, SQL optimization, analysis & reporting patterns. `references/` tree stays.

**STRIP:**
- "Injected when role=data" (C9).
- `metadata: { "keywords": [...] }`.
- Any cli-jaw runtime references.

**REWRITE:**
- Description → `"MUST USE for data engineering and analysis — data pipelines, ETL/ELT design, data quality validation, SQL optimization, analysis patterns. Triggers: ETL, ELT, pipeline, data quality, SQL optimization, backfill, migration, data validation, schema drift."`
- Cross-skill refs to `dev-backend` → keep as codexclaw names.

---

### 2.6 `dev-debugging` — Root Cause Analysis  [R]

**Classification:** ROUTER. This is the recipe anchor (024.1) — convert first to lock the recipe.

**KEEP:** 5-phase methodology (architecture check → investigate → analyze → hypothesize → implement), boundary instrumentation, hypothesis testing, postmortem. The `references/` split stays.

**STRIP:**
- `metadata: { "keywords": [...] }` — dev-debugging frontmatter has none (it uses bare `description`), so this is already clean.
- Any cli-jaw cross-refs (minimal per 024.2: LOW severity).

**REWRITE:**
- Description → omo-style trigger dictionary: `"MUST USE for any real runtime debugging — crashes, silent failures, wrong responses, stuck processes, unexplained timing. Runs a hypothesis-driven loop: form hypotheses, investigate, confirm root cause, lock with a failing test, fix minimally. Triggers: debug this, why is X not working, hanging, trace this bug, reproduce and fix, root cause, silent failure, 3+ failed fix attempts."`
- Model the SKILL.md body on omo's `debugging/SKILL.md` router pattern: small top-level file, "the knowledge is in references/" gate rule, progressive disclosure.
- `agents/openai.yaml` with `allow_implicit_invocation: false`.

---

### 2.7 `dev-devops` — Container/Deploy/K8s  [R]

**Classification:** ROUTER. Activated for container builds, deploy pipelines, K8s, IaC, SRE.

**KEEP:** Container build patterns, deploy pipeline design, Kubernetes patterns, IaC (Terraform/Pulumi), SRE foundations, edge/serverless, ML infrastructure. `references/` tree stays.

**STRIP:**
- "Injected when task_tags include devops/infra/deploy/release_cd" → reword to "routes when work touches containers, deploy, K8s, IaC, or SRE."
- `metadata: { "keywords": [...] }`.
- Any cli-jaw-specific CI references (verify-counts.sh, dist/ build contract) (C5).

**REWRITE:**
- Description → `"MUST USE for DevOps — container builds, deploy pipelines, Kubernetes, Infrastructure as Code, SRE foundations, edge/serverless, ML infrastructure. Triggers: Dockerfile, container build, deploy, CI/CD, Kubernetes, K8s, Terraform, Pulumi, Helm, SRE, SLI, SLO, error budget, serverless, edge."`
- Strip any `dist/` / `verify-counts.sh` / cli-jaw build-contract references — these are cli-jaw repo rules, not universal (C5).

---

### 2.8 `dev-frontend` — UI Implementation  [R]

**Classification:** ROUTER. Activated for UI components, CSS, responsive layouts, animation.

**KEEP:** Component architecture, CSS/responsive patterns, animation rules, anti-slop detection, product density profiles, mobile-ux. The `references/core/` tree (anti-slop.md, mobile-ux.md, product-density.md, etc.) stays.

**STRIP:**
- "Injected when role=frontend" (C9).
- `metadata: { "keywords": [...] }`.
- C5 coupling: `structure/`, `devlog/` house rules if present in this skill.

**REWRITE:**
- Description → `"MUST USE for ANY frontend, web UI, or visual implementation work — building, styling, or redesigning pages/components, responsive layouts, animation, component architecture. Triggers: frontend, UI, component, CSS, responsive, animation, React, Vue, Svelte, Tailwind, layout, styling, redesign, mockup, anti-slop."`
- §14 production-surface checklist → keep the "production surface" definition (universal).
- Cross-ref to `dev-uiux-design` for design judgment → keep as codexclaw name.
- §5 Emoji ban stub → keep, point to `dev-frontend/references/core/anti-slop.md`.

---

### 2.9 `dev-pabcd` — PABCD Orchestration  [SPECIAL — see §4]

**Classification:** NOT A SEPARATE SKILL. Folds into codexclaw `pabcd` (015.1, 024 Q-DEV-2).

**STRIP (entire skill as standalone):**
- All `cli-jaw orchestrate I/P/A/B/C/D` commands (C3).
- `cli-jaw orchestrate reset` (C3).
- `cli-jaw dispatch --agent` / `--mutable` / `--scope` (C4).
- `cli-jaw bgtask add` in C phase (C7).
- `cli-jaw project add/clear` (C10).
- Evidence gate `--attest` JSON commands (server-gated, no server in codex).
- Jawdev decade numbering as a hard rule (C5).
- "Boss agent" / "server re-invokes the boss" (C4).

**REWRITE (absorb into `pabcd`):**
- See §4 below for the full special-case treatment.

---

### 2.10 `dev-scaffolding` — Project/Module Scaffold  [R]

**Classification:** ROUTER. Activated for new project setup, feature module scaffold, structural audit.

**KEEP:** Colocation, public boundary export, barrel export discipline, documentation generation patterns. The Lidge Standard *principles* (feature-based + colocation) are universal.

**STRIP:**
- "Lidge Standard" as a branded name → keep the principles, drop the brand if desired (judgment call; the name is harmless but cli-jaw-specific).
- `devlog/` as a mandatory artifact (C5) — soften to "project source-of-truth logs."
- `structure/` as a mandatory folder (C5).
- `verify-counts.sh`, `dist/` build contract (C5).
- `metadata: { "keywords": [...] }`.

**REWRITE:**
- Description → `"MUST USE for new project or feature module setup, structural audit, or docs generation — feature-based colocation, public boundary export, barrel discipline, source-of-truth structure. Triggers: scaffold, new project, new feature, init project, audit structure, add module, project setup, colocation, barrel export."`
- "MUST NOT create structure/, devlog/, AGENTS.md silently" → keep the universal principle ("don't scaffold project-level structure without approval") but drop the cli-jaw-specific folder names as hard requirements.
- devlog phase docs decade numbering → optional guidance.

---

### 2.11 `dev-security` — Auth/Secrets/Validation  [R]

**Classification:** ROUTER. Activated for auth, secrets, validation, supply chain, security review.

**KEEP:** OWASP Top 10, auth hardening, input validation, secrets management, supply chain security, threat modeling. Nearly fully universal (LOW severity per 024.2).

**STRIP:**
- "Injected when security/threat_model task_tags" → reword to surface-triggered (C9).
- `metadata: { "keywords": [...] }`.

**REWRITE:**
- Description → `"MUST USE for security-sensitive code — auth, secrets, input validation, supply chain, security review, threat modeling. Triggers: auth, authentication, authorization, OAuth, JWT, secrets, API keys, input validation, XSS, SQL injection, OWASP, threat model, security review, dependency audit, Semgrep."`
- Cross-ref to `dev-architecture §4` boundary-only defense → keep as codexclaw name.

---

### 2.12 `dev-testing` — Test Strategy  [R]

**Classification:** ROUTER. Activated for testing, TDD, coverage, contract tests, E2E.

**KEEP:** Test strategy selection, backend/API harnesses, contract verification, Playwright E2E, CI pipelines, TDD enforcement, security scanning, coverage gates. The `references/` tree stays.

**STRIP:**
- "Injected when testing/tdd task_tags, or testing phase" → reword to surface-triggered (C9).
- `metadata: { "keywords": [...] }`.
- Any cli-jaw CI-specific references.

**REWRITE:**
- Description → `"MUST USE for testing — test strategy, backend/API harnesses, contract verification, Playwright E2E, CI pipelines, TDD enforcement, coverage gates, security scanning. Triggers: test, testing, TDD, unit test, integration test, contract test, E2E, Playwright, coverage, test strategy, red-green, fixture."`
- §6 Edge-first testing, §6.6 test-induced defense → keep (canonical owner per dev Ownership Map).
- Cross-refs to `dev` §3 verification gate → keep as codexclaw `dev` reference.

---

### 2.13 `dev-uiux-design` — Design Intent/UX  [R]

**Classification:** ROUTER. Activated for vague design direction, onboarding/empty/error states, favicon/logo work.

**KEEP:** User Intent Discovery Protocol (6-step Socratic Scaffolding), Design Read, Korean Request Translation table, Quick-Match table, Font Selection Guidelines, product personalities, design-isms, layout macrostructures, UX states, color system, responsive nav, typography line breaks, favicon/logo, logo trust sections, visual hierarchy, form patterns, mobile-native UX. The `references/` tree stays. This skill is nearly fully universal (MED severity, mostly structure refs).

**STRIP:**
- `keywords: [...]` in frontmatter (dev-uiux uses bare `keywords:` list, not nested metadata — still not read by codex loader, C1).
- "Read this BEFORE `aesthetics.md`" → reword to reference codexclaw `dev-frontend` instead.
- C5 structure refs if any.

**REWRITE:**
- Description → `"MUST USE for UI/UX design — intent discovery, design vocabulary, product personalities, UX state patterns, typography line breaks, favicon/product logo design, logo trust sections. Triggers: design direction, 깔끔하게, 모던하게, onboarding, empty state, error state, loading state, favicon, logo, brand identity, OG image, dark mode logo, product aesthetic, Notion 느낌, Linear처럼."`
- Role separation note: "This skill provides design judgment; `dev-frontend` provides implementation" → keep, both are codexclaw routers.
- §1 UX-STYLE-01 rule class note → keep (STYLE_SAMPLE classification is universal).

---

## 3. SKILL.md Frontmatter Mapping

codex-rs `SkillFrontmatter` (loader.rs:40-58) accepts ONLY: `name`, `description`, `metadata.short-description`.
`deny_unknown_fields` is NOT set on `SkillFrontmatter` itself (it uses `#[serde(default)]` per field), but `SkillMetadataFile` / `Policy` use `deny_unknown_fields` on some sub-structs. Best practice: emit ONLY accepted fields.

| cli-jaw frontmatter | codexclaw target | Notes |
|---|---|---|
| `name: dev-backend` | `name: dev-backend` | Same; ≤64 chars (loader.rs MAX_NAME_LEN). |
| `description: "API design..."` | `description: "MUST USE for ... <triggers>"` | ≤1024 chars (MAX_DESCRIPTION_LEN). This is the PRIMARY routing driver. |
| `metadata: { "keywords": [...] }` | **DROP** — not read (loader.rs:50-54) | Move intent into description + short-description. |
| (none) | `metadata: { short-description: "..." }` | ≤1024 chars. One-liner for display. |
| (none) | `agents/openai.yaml` sidecar | Optional. Holds `interface.display_name`, `interface.default_prompt`, `policy.allow_implicit_invocation`, `dependencies.tools`. |

**openai.yaml per-skill policy:**

| Skill | allow_implicit_invocation | Rationale |
|---|---|---|
| `dev` | `true` | Always-on universal discipline (C2). |
| `dev-architecture` | `false` | On-demand router. |
| `dev-backend` | `false` | On-demand router. |
| `dev-code-reviewer` | `false` | On-demand router. |
| `dev-data` | `false` | On-demand router. |
| `dev-debugging` | `false` | On-demand router. |
| `dev-devops` | `false` | On-demand router. |
| `dev-frontend` | `false` | On-demand router. |
| `dev-scaffolding` | `false` | On-demand router. |
| `dev-security` | `false` | On-demand router. |
| `dev-testing` | `false` | On-demand router. |
| `dev-uiux-design` | `false` | On-demand router. |
| `pabcd` (absorbs dev-pabcd) | `false` | Explicit trigger or hook-driven (hook.ts detectTrigger). |

**omo exemplar pattern (to follow):**

omo `debugging/SKILL.md` frontmatter:
```yaml
---
name: debugging
description: "MUST USE for any real runtime debugging across ANY language or binary — crashes, silent failures, wrong responses... Triggers: 'debug this', 'why is X not working', ..."
---
```
Note: omo omits `metadata.short-description` for some skills (it's optional). codexclaw SHOULD include it for display consistency. omo's `agents/openai.yaml` carries `search_terms` which codex-rs `Interface` does NOT define (021.1) — codexclaw should NOT rely on `search_terms`; use `description` for routing.

---

## 4. dev-pabcd Special Case — Rewrite Against codexclaw FSM

dev-pabcd is the HIGHEST-conflict skill (024.2): it is entirely built around `cli-jaw orchestrate` server commands. Per 015.1, it does NOT ship as a separate skill — its intent is absorbed by the existing codexclaw `pabcd` skill (`plugins/codexclaw/skills/pabcd/SKILL.md`) + the `pabcd-state` component hook.

### 4.1 What to port from dev-pabcd into `pabcd`

| dev-pabcd content | Port action | Target |
|---|---|---|
| Phase definitions P/A/B/C/D (dev-pabcd/SKILL.md:Phases) | KEEP — already in `pabcd/SKILL.md` Phases section | `pabcd/SKILL.md` (already present) |
| Interview trigger + 4 dimensions (dev-pabcd/SKILL.md:Interview Trigger) | KEEP — already in `pabcd/SKILL.md` Phase 0 (I) | `pabcd/SKILL.md` (already present) |
| `cli-jaw orchestrate I/P/A/B/C/D` commands | REWRITE → hook-driven `detectTrigger()` (hook.ts:55-63) | `pabcd-state/src/hook.ts` (already implemented) |
| `cli-jaw orchestrate reset` | REWRITE → clear state file (`.codexclaw/state.json`) | `pabcd-state/src/cli.ts` / `state.ts` |
| Evidence gate `--attest` JSON | REWRITE → text attestation in phase directives (hook.ts:25-48 PHASE_DIRECTIVES) | `pabcd-state/src/hook.ts` (C→D directive already demands "fresh command output as evidence") |
| `cli-jaw dispatch --agent` for A-phase audit | REWRITE → `multi_agent_v1.spawn_agent` (A directive: "Dispatch an independent reviewer (sub-agent)") | `pabcd/SKILL.md` + hook.ts A directive (already present) |
| `cli-jaw bgtask add` in C phase | REWRITE → background subagent + `wait_agent` | `pabcd/SKILL.md` C phase |
| Jawdev decade numbering | STRIP as hard rule → optional guidance | `pabcd/SKILL.md` Notes |
| Repository Root Contract (`cli-jaw project add`) | REWRITE → resolve via `pwd -P` / AGENTS.md; state in subagent task | `pabcd/SKILL.md` |
| PABCD Depth by Work Class table (C0-C5) | KEEP — universal, references `dev` §0.0 classifier | `pabcd/SKILL.md` (add if not present) |
| work-phase vs PABCD-phase terminology | KEEP — universal | `pabcd/SKILL.md` (add if not present) |
| Faithful execution (anti-skip) | KEEP — universal | `pabcd/SKILL.md` (add if not present) |
| Delegation Trap (Boss writes, workers read-only unless --mutable) | REWRITE → "main agent writes; subagents are read-only verifiers unless spawned with write scope" | `pabcd/SKILL.md` |

### 4.2 Current state of `pabcd/SKILL.md`

The existing `pabcd/SKILL.md` (already written) covers: phases I/P/A/B/C/D, state file locations, hook-driven trigger detection. It is the MVP guide. The dev-pabcd content to ADD (not yet present):

1. **PABCD Depth by Work Class** table — port from dev-pabcd, referencing `dev` skill §0.0 classifier.
2. **work-phase vs PABCD-phase** terminology + invariant (one work-phase = one full PABCD cycle).
3. **Faithful execution (anti-skip)** — do the real work of each phase, don't rubber-stamp.
4. **Delegation model** — main agent writes in B; subagents verify (read-only by default).
5. **Long external verification** — background subagent + `wait_agent` (replace bgtask).
6. **Repository root resolution** — `pwd -P`, state in subagent task bodies.

### 4.3 Hook directive alignment

The `pabcd-state/src/hook.ts` `PHASE_DIRECTIVES` (hook.ts:25-48) already encode the ported intent:

cli-jaw original (dev-pabcd/SKILL.md:Interview Trigger):
> you MUST immediately run: `cli-jaw orchestrate I`

codexclaw converted (hook.ts:28-33, I directive):
```
[codexclaw: INTERVIEW]
Clarify requirements before planning. Cover four dimensions — Goal, Constraint,
Success criteria, Ontology. Research the repo first, then ask focused questions.
Do NOT start implementing yet.
```

cli-jaw original (dev-pabcd/SKILL.md:C→D attestation):
> `cli-jaw orchestrate D --attest '{"from":"C","to":"D","did":"<what you checked>","checkOutput":"<paste the real tsc/test tail>","exitCode":0}'`

codexclaw converted (hook.ts:42-47, C directive):
```
[codexclaw: CHECK]
Run the real verification: tests, type checks, and adversarial review. Capture fresh
command output as evidence. Do not claim pass without artifact-level proof.
```

The evidence-gate intent (force a deliberate, specific claim) is preserved as a text directive rather than a server-gated command. Pass 5 may harden this into a PreToolUse/Stop hook that checks for evidence artifacts (see §5).

---

## 5. Pass Assignment + Ordering

codexclaw Phase 1 has two implementation passes (per the plan structure):
- **Pass 4** = dev-* skill conversion (the 13 skills → codexclaw skills/).
- **Pass 5** = pabcd hardening + integration polish (evidence gates, subagent role wiring, verification).

### 5.1 Pass 4 — dev-* skill conversion (all 13)

**Sequencing (from 024.1 recipe anchor):**

| Order | Skill | Why this order |
|---|---|---|
| 1 | `dev-debugging` | Recipe anchor (024.1) — convert end-to-end first to lock the recipe. LOW conflict (024.2). |
| 2 | `dev-testing` | Validate recipe reproduces on a 2nd skill before bulk. LOW conflict. |
| 3 | `dev-security` | LOW conflict, fast. |
| 4 | `dev-code-reviewer` | LOW-MED, fast. |
| 5 | `dev-architecture` | MED — module rules, minor devlog refs. |
| 6 | `dev-data` | MED — pipelines, minor refs. |
| 7 | `dev-uiux-design` | MED — mostly universal, frontmatter rewrite. |
| 8 | `dev-frontend` | MED-HIGH — C5 structure/devlog coupling. |
| 9 | `dev-backend` | MED-HIGH — C4/C5 coupling. |
| 10 | `dev-devops` | MED-HIGH — C5 coupling (dist/, verify-counts). |
| 11 | `dev-scaffolding` | HIGH — C5 (Lidge/devlog) heavy. |
| 12 | `dev` (common) | HUB — rewrite classifier + discipline, strip all cli-jaw plumbing. Last because it references all others. |
| 13 | `pabcd` (absorb dev-pabcd) | MERGE — add Depth-by-Class, terminology, anti-skip, delegation model to existing `pabcd/SKILL.md`. |

**Pass 4 deliverable per skill:**
- `skills/<name>/SKILL.md` — converted frontmatter + body (stripped/rewritten per §2).
- `skills/<name>/references/*.md` — ported from cli-jaw (content is mostly universal; strip cli-jaw paths).
- `skills/<name>/agents/openai.yaml` — routing metadata + `allow_implicit_invocation` per §3.
- Verify: skill validates against loader.rs schema; routes on a representative prompt; zero cli-jaw paths remain (`rg "cli-jaw|orchestrate|dispatch|bgtask|devlog/|verify-counts" skills/` = empty).

### 5.2 Pass 5 — pabcd hardening + integration

| Item | Source | Target |
|---|---|---|
| Evidence-gate hardening (C→D checks for artifact-level proof) | dev-pabcd Evidence gate | `pabcd-state/src/hook.ts` Stop handler (currently PASSIVE no-op, hook.ts:104-107) → add ledger/evidence check |
| Subagent role wiring (A-phase audit subagent) | dev-pabcd A phase + 025 | codex agent role `.toml` files (auditor role) + `pabcd/SKILL.md` A directive |
| Long external verification pattern | dev-pabcd C phase bgtask | `pabcd/SKILL.md` C phase + dev §3 (already rewritten in Pass 4) |
| FSM auto-advance (Stop hook → ledger) | hook.ts:104 TODO | `pabcd-state/src/hook.ts` handleStop (deferred per 018.2) |
| Cross-skill routing verification | 024 B-opt2 | Integration test: spawn subagent with inline dev-* router ref, verify it loads |

**Pass 5 ordering:** evidence-gate → subagent roles → FSM auto-advance → integration test.

---

## Summary

This delta maps every cli-jaw-ism in the 13 dev-* skills to a concrete codex-native rewrite. The universal discipline (work classifier, modular limits, verification gate, safety rules) stays intact; cli-jaw runtime commands (`orchestrate`, `dispatch`, `bgtask`, `project`) are ported to codex equivalents (FSM hooks, `spawn_agent`, background subagent polling, cwd config). `dev` is always-on; the 12 surface skills are on-demand routers; `dev-pabcd` folds into `pabcd`. Pass 4 converts all 13 (debugging first as recipe anchor); Pass 5 hardens evidence gates and subagent wiring.
