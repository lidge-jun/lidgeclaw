# L9.3 (Decade 093) -- Loop Coordinator T4

Status: DONE
Cluster: 1 - IPABCD Interview completion - Phase: 1 - Shorthand: cxc
Source-of-record: 080.1, 080.2, 034.5, ouroboros 030/040

## Goal (one slice)
Lock the interview loop owner as the main agent and keep the hook limited to directive injection.

## Why now / dependencies
L8 gives persistent state. The Pass-2 hook shape can inject text on `UserPromptSubmit`, but cannot observe user-input answers, Mind results, plan edits, or every loop transition.

## Scope (decision-complete)
- Files to add/edit: `plugins/codexclaw/components/pabcd-state/src/hook.ts`, `.codexclaw/` state/plan files by runtime behavior, and matching tests.
- T4 is resolved: the main agent owns the prompt-only interview loop.
- The hook only injects directives such as interview mode active, current stage, gates, and next expected action.
- State lives in `.codexclaw/` files, including session tracker data and `.codexclaw/plan/` plan artifacts.
- Main runs the cycle: read tracker and plan -> select Minds -> dispatch contradiction workers -> triage contradictions -> ask user if needed -> edit plan -> update state -> re-question.
- Hook must not claim to coordinate answers, worker returns, or plan edits.
- Tests verify directive and state-helper guarantees, not hidden runtime behavior.
- Must-NOT-Have: no real loop state API in MVP, no subagent plan edits, no user I/O from workers.

## IPABCD micro-cycle
- I: Main owns one bounded round at a time and records the outcome in `.codexclaw/`.
- P: Encode T4 resolution in directive text: main-owned loop, hook as injector only.
- A: Reviewer checks every transition has an owner and no hook capability is overstated.
- B: Implement directive text and optional state-file naming notes; keep code surface small.
- C: Run hook/state tests for directive content and readiness invariants.
- D: Done = loop ownership is explicit and the hook never promises observations it cannot make.

## Acceptance (1-3 testable criteria)
1. Directive says the main agent owns question, dispatch, plan edit, state update, and re-question.
2. Directive says hook is directive injection only.
3. `.codexclaw/` is the named state and plan artifact surface.

## QA channel (node:test path / CLI stdout / tmux / data dump)
- `node --test plugins/codexclaw/components/pabcd-state/test/hook.test.ts`
- Optional CLI stdout: `cxc interview minds --dry-run` if introduced.

## Commit unit (one atomic conventional commit)
`feat(interview): document main-owned interview loop`

## Blocked-on (jun decision id, if any)
None. T4 resolved to Option A: main-agent-owned prompt-only loop; hook injects directives only.

## References (codex-rs paths, omo skills, ouroboros, source-of-record docs)
- `devlog/_plan/260629_codexclaw_mvp/080.2_interview_ux_and_loop_method.md`
- `devlog/_plan/260629_codexclaw_mvp/080.1_interview_contradiction_register.md`
