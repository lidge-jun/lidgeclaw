# L4.1 (Decade 041) -- Skill Schema and Policy

Status: DONE
Cluster: 1 - Phase: 1 - Shorthand: cxc
Source-of-record: 024_dev_skills_conversion.md; 024.3_skill_conversion_delta.md; 024.4_pass4_P_plan.md

## Goal (one slice)
Normalize every Phase 1 skill directory to the Codex skill schema and invocation policy.

## Why now / dependencies
Before router content could be trusted, cxc needed skills the Codex loader can discover. This
sub-pass creates the schema and policy baseline for L4.2 and L4.3.

## Scope (decision-complete)
- Files added/edited:
  - `plugins/codexclaw/skills/*/SKILL.md`
  - `plugins/codexclaw/skills/*/agents/openai.yaml`
  - `plugins/codexclaw/skills/README.md`
- Skill count:
  - exactly 13 Phase 1 skill directories.
  - `dev` hub.
  - 11 dev router skills.
  - `pabcd` workflow skill.
- Schema policy:
  - `SKILL.md` frontmatter uses `name`, `description`, and `metadata.short-description`.
  - descriptions stay within Codex loader limits.
  - copied `references/`, `scripts/`, `assets/`, and `examples/` remain beside their skill.
- Invocation policy:
  - `plugins/codexclaw/skills/dev/agents/openai.yaml` has `allow_implicit_invocation: true`.
  - all routers and `pabcd` have `allow_implicit_invocation: false`.
- Must-NOT-Have:
  - No `keywords` or `search_terms` frontmatter.
  - No second implicit skill.
  - No dangling copied reference path.

## IPABCD micro-cycle
- I (if interview-bearing): not interview-bearing.
- P: use Codex skill loader shape, not cli-jaw skill metadata.
- A: Pass 4 audit found imprecise grep gates and incomplete whole-directory copying; plan was revised.
- B: ported whole skill directories and added per-skill `agents/openai.yaml`.
- C: Pass 4 C-gate checked frontmatter validity and policy; L7 build validator checked `SKILL.md` exists.
- D: done = all 13 skills are discoverable and only `dev` is implicit.

## Acceptance (1-3 testable criteria)
- `find plugins/codexclaw/skills -mindepth 1 -maxdepth 1 -type d` returns 13 directories.
- Every skill dir contains `SKILL.md`.
- Exactly one policy file has `allow_implicit_invocation: true`: `dev`.

## QA channel (node:test path / CLI stdout / tmux / data dump)
- Pass 4 C-gate: frontmatter valid and precise cli-jaw gate empty.
- L7 build test: `every manifest-referenced dist + skill path exists post-build`.
- Final Phase 1 regression: root `npm test` 73/73.

## Commit unit (one atomic conventional commit)
Policy and skill-schema work spans Pass 4 commits e8cdec0, 323db78, 7e1575b, c7e8300, 7f53ab5,
3e24848, and 2483886; `pabcd` implicit false was corrected in ba20b64.

## Blocked-on (jun decision id, if any)
None.

## References (codex-rs paths, omo skills, ouroboros, source-of-record docs)
- `plugins/codexclaw/skills/README.md`
- `plugins/codexclaw/skills/dev/agents/openai.yaml`
- `plugins/codexclaw/skills/pabcd/agents/openai.yaml`
- `devlog/_plan/260629_codexclaw_mvp/024.4_pass4_P_plan.md`
- codex-rs `core-skills/loader.rs` frontmatter constraints.
