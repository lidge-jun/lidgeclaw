# L6.3 (Decade 063) -- Deactivate and CLI Wiring

Status: DONE
Cluster: 1 - Phase: 1 - Shorthand: cxc
Source-of-record: 028_phase1_integration.md; 028.1_install_activation.md; 160_pass1_7_completeness_audit.md

## Goal (one slice)
Expose `cxc enable`, `cxc status`, and `cxc uninstall`, with drift-safe deactivation.

## Why now / dependencies
L6.2 records what cxc changed. This sub-pass completes the user-facing CLI path and fixes the B2
wiring gap found after the component was already tested.

## Scope (decision-complete)
- Files added/edited:
  - `plugins/codexclaw/components/config-guard/src/deactivate.ts`
  - `plugins/codexclaw/components/config-guard/src/cli.ts`
  - `bin/codexclaw.mjs`
  - `plugins/codexclaw/components/config-guard/test/activate.test.ts`
- Functions shipped:
  - `deactivate(deps)`
  - `resolveCodexHome(env)`
  - `assertNotRealCodexHome(path, env)`
  - `makeRealRunner()`
- CLI commands:
  - `cxc enable`
  - `cxc status`
  - `cxc uninstall`
- Deactivation behavior:
  - safe no-op when manifest is missing.
  - refuses to revert when config hash drift is detected.
  - disables only flags listed as enabled by codexclaw.
  - preserves pre-existing true flags.
- Must-NOT-Have:
  - No blind restore over user-edited config.
  - No root CLI stub for activation.
  - No real-home mutation in tests.

## IPABCD micro-cycle
- I (if interview-bearing): not interview-bearing.
- P: expose config-guard through root cxc rather than leaving component-only logic.
- A: 160 audit found root CLI did not call config-guard; B2 was a real end-to-end gap.
- B: wired `bin/codexclaw.mjs` to compiled config-guard dist commands and implemented deactivation.
- C: tests cover missing manifest no-op, drift refusal, real-home guard, and enable/disable roundtrip.
- D: done = activation is reachable through cxc and uninstall is safe.

## Acceptance (1-3 testable criteria)
- `cxc enable` delegates to config-guard activation.
- `cxc uninstall` reverts only cxc-enabled flags and refuses drift.
- `cxc status` prints live declared feature state through the config-guard CLI.

## QA channel (node:test path / CLI stdout / tmux / data dump)
- `plugins/codexclaw/components/config-guard/test/activate.test.ts`: 7 tests.
- Relevant tests: enable then disable restores prior state, no-manifest safe no-op,
  drift detection, real `~/.codex` guard.
- Wiring fix evidence: ba20b64; final root `npm test` 73/73 and build OK.

## Commit unit (one atomic conventional commit)
Deactivate component landed in 4a1c08f; root cxc wiring landed in ba20b64.

## Blocked-on (jun decision id, if any)
None.

## References (codex-rs paths, omo skills, ouroboros, source-of-record docs)
- `plugins/codexclaw/components/config-guard/src/deactivate.ts`
- `plugins/codexclaw/components/config-guard/src/cli.ts`
- `bin/codexclaw.mjs`
- `devlog/_plan/260629_codexclaw_mvp/160_pass1_7_completeness_audit.md`
