# L30.2 (Decade 302) -- Phase 3 Verification

Status: DEFERRED (Q-P3-2, Phase 3)
Cluster: 5 - Phase: 3 - Shorthand: cxc
Source-of-record: 300_L30_result_delivery_phase3_verify.md, 260629_codexclaw_mvp/040_phase3_overview.md

## Goal (one slice)
Prove Phase 3 end to end: a `cxc schedule` job can produce a `codex exec` run
artifact and the result can be inspected through cxc.

## Why now / dependencies
- Upstream: depends on L29 scheduling and L30.1 result capture.
- Downstream: closes Phase 3 as a shippable MVP unit and provides the contract
  L31 channel delivery forwards.

## Scope (decision-complete except Q-P3-2)
- Files to add/edit:
  - verification test or script under scheduler tests
  - `cxc schedule verify --dry-run` implementation
  - `devlog/_fin/` done note only after implementation actually completes
- Exact behavior:
  - `cxc schedule verify --dry-run` exercises the generated scheduler path
    without waiting for a real timer.
  - One direct local run can capture a harmless `codex exec` result with
    codexclaw active.
  - Verification records both success and non-zero failure evidence.
- Must-NOT-Have:
  - No claim of Phase 3 DONE while Q-P3-1 or Q-P3-2 remains unresolved.
  - No real channel credentials.
  - No reliance on server-owned bgtask.

## IPABCD micro-cycle
- I: not interview-bearing.
- P: define verification transcript, dry-run command, fixture run records, and
  direct local command evidence.
- A: audit angle = "does verification prove scheduler-to-result, not just store
  writes?" Reviewer compares command path to L29 generated artifact.
- B: implement verify dry-run, seeded fixtures, direct-run capture path, and
  summary output.
- C: run `cxc schedule verify --dry-run`; run targeted node:test; inspect stored
  run fixture and CLI output.
- D: done = Phase 3 has reproducible evidence for scheduling plus result
  inspection.

## Acceptance (1-3 testable criteria)
1. Verify dry-run prints scheduler command, expected store writes, and result
   capture path.
2. A direct local run writes an inspectable result record.
3. Failure evidence is preserved and not reported as success.

## QA channel (node:test path / CLI stdout / tmux / data dump)
- CLI stdout: `cxc schedule verify --dry-run`.
- node:test for verification fixture behavior.
- Data dump: `.codexclaw/` run result fixture.

## Commit unit (one atomic conventional commit)
`test(schedule): verify phase 3 scheduling flow`

## Blocked-on (jun decision id, if any)
BLOCKED(Q-P3-2): scheduled-result delivery destination.

Recommendation: project-local file store. Impact: deterministic verification,
usable failure triage, and a clean bridge to L31 channel forwarding.

## References (codex-rs paths, omo skills, ouroboros, source-of-record docs)
- devlog/_plan/mvp_res/300_L30_result_delivery_phase3_verify.md
- devlog/_plan/260629_codexclaw_mvp/040_phase3_overview.md (044 verification)
- devlog/_plan/mvp_res/290_L29_scheduler_mechanism_cli.md
- devlog/_plan/mvp_res/301_L30.1_result_delivery.md
