# L11.2 (Decade 112) -- PreToolUse Hard Deny Q-GM-1-f

Status: DONE (resolved jun 2026-06-30; shipped + tested)
Cluster: 1 - IPABCD Interview completion - Phase: 1 - Shorthand: cxc
Source-of-record: 022.3, 023, 023.1, 080

## Resolved (jun 2026-06-30)
goal-mode 판정 소스 = L11.1의 read-only `goals_1.sqlite` 조회(thread_id). 자체 마커 없음.
이 deny가 필요한 이유: codex의 goal 게이트 억제는 PARTIAL(continuation 프롬프트만; `request_user_input`
비활성화 없음 — `codex-rs/core/src/tools/handlers/request_user_input_spec.rs:85`). codexclaw가 직접 막는다.

## 보강 (007 findings R-9/R-10) -- fail-closed strict deny + narrow matcher
- **R-9 fail-closed**: cli.ts의 글로벌 try/catch는 모든 IO 실패를 삼켜 exit-0(fail-safe)이다. 그러면 goal-active
  판정 불가 시 deny가 사라진다. → PreToolUse deny는 **strict 경로로 분리**: goal-active 조회가 `unreadable`이면
  fail-CLOSED(=deny)로 처리하고, 이 경로는 글로벌 fail-safe 래퍼에 삼켜지지 않게 한다. (jawcode strict-writer parity)
- **R-10 narrow matcher**: 매 툴마다 hook 돌리지 말고 plugin.json PreToolUse에 `request_user_input`(및 L3의
  `^create_goal$`) matcher만 건다(omo hooks.json:15 parity).

## Goal (one slice)
Deny `request_user_input` in goal mode using the L3 PreToolUse mechanism, keyed off the L11.1 native goal-active lookup (`goals_1.sqlite` by thread_id).

## Why now / dependencies
L11.1 supplies deterministic goal-mode detection (read-only native goal DB). L3 already established the PreToolUse hard-deny pattern (deny envelope shape) for goal budget gating.

## Scope (decision-complete)
- Files to add/edit: PreToolUse matcher configuration owned by the codexclaw plugin, `plugins/codexclaw/components/pabcd-state/src/hook.ts`, `plugins/codexclaw/components/pabcd-state/src/cli.ts`, `plugins/codexclaw/components/pabcd-state/test/hook.test.ts`, `plugins/codexclaw/components/pabcd-state/test/cli.test.ts`.
- Reuse the exact L3 goal-budget-gate PreToolUse mechanism and output shape.
- Deny tool calls whose name is exactly `request_user_input` when L11.1 reports goal-active (native `goals_1.sqlite` row `status = Active` for this thread_id).
- Deny output uses `permissionDecision: deny` with a clear reason: goal mode forbids interview/user input because autonomous execution must not block on the user.
- Layer 1 defense: PreToolUse hard deny gated on the native goal-active lookup.
- Layer 2 defense: advisory skill/directive text still tells the agent never to interview or call `request_user_input` in goal mode.
- With goal active, suppress I-phase directive injection and do not create or update `state.interview`.
- Goal-mode missing facts use autonomous backfill only: verified fact, contradiction, or assumption.
- User-only gaps become high-severity assumptions requiring user review, not questions.
- Must-NOT-Have: no quick interview fallback, no plain-text question fallback, no new I-phase goal type, no prompt-only hard-deny claim, no self-built goal marker.

## IPABCD micro-cycle
- I: Active goal mode never enters I; defer interview until the native goal is no longer `Active` (goals_1.sqlite lookup).
- P: Add PreToolUse matcher and advisory text using L3 pattern.
- A: Security reviewer checks direct tool calls, unreadable goal-row fail-closed behavior, and backfill disguised as interview.
- B: Implement deny matcher, suppression path, diagnostics, and tests.
- C: Run hook/CLI tests plus a synthetic PreToolUse event.
- D: Done = native goal-active (Active row) denies `request_user_input`, suppresses Interview, and preserves advisory defense.

## Acceptance (1-3 testable criteria)
1. Goal-active (native DB `Active` row) PreToolUse for `request_user_input` returns `permissionDecision: deny` with explicit goal-mode reason.
2. Goal-active interview trigger does not inject interview directive or create `state.interview`.
3. Goal-inactive (no DB / no row / non-Active) state allows L8-L10 interview behavior.

## QA channel (node:test path / CLI stdout / tmux / data dump)
- `node --test plugins/codexclaw/components/pabcd-state/test/hook.test.ts`
- `node --test plugins/codexclaw/components/pabcd-state/test/cli.test.ts`
- CLI stdout: `cxc goal doctor`

## Commit unit (one atomic conventional commit)
`fix(goal): deny user input while native goal is active`

## Blocked-on (jun decision id, if any)
None. Q-GM-1-f RESOLVED: two-layer defense = PreToolUse deny gated on native goal-active DB lookup (L11.1) + advisory text.

## Hardening pins (Pass 1, jun 2026-06-30)
- PreToolUse matcher config path is EXACT:
  `plugins/codexclaw/hooks/pre-tool-use-guarding-interview-in-goal.json` (new, dedicated), registered
  in `plugins/codexclaw/.codex-plugin/plugin.json` after `pre-tool-use-guarding-goal-budget.json`.
- Deny target tool name = `request_user_input` (exact match). Fail-closed on `unreadable` goal DB.

## References (codex-rs paths, omo skills, ouroboros, source-of-record docs)
- `devlog/_plan/260629_codexclaw_mvp/022.3_interview_goalmode_rules.md`
- `devlog/_plan/260629_codexclaw_mvp/023_goal_convention_port.md`
- `codex-rs/protocol/src/request_user_input.rs`
- `codex-rs/core/src/tools/handlers/request_user_input_spec.rs:85` (mode/feature based, not goal based)
- `codex-rs/hooks/src/schema.rs:239` (PreToolUse deny envelope shape, reused from L3)
- L3 goal-gate impl: `plugins/codexclaw/components/pabcd-state/src/goal-gate.ts` (deny envelope pattern)
