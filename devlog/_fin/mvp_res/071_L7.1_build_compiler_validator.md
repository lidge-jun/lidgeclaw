# L7.1 (Decade 071) -- Build Compiler and Validator

Status: DONE
Cluster: 1 - Phase: 1 - Shorthand: cxc
Source-of-record: 070_packaging.md; 070.1_pass7_P_plan.md; 029.1_phase1_verification_done.md

## Goal (one slice)
Compile component TypeScript to tracked runtime JavaScript and validate the plugin layout.

## Why now / dependencies
L7 aggregates L1-L6. The Codex plugin runs command hooks from installed files, so cxc needs committed
`dist/` artifacts and a validator that proves hook paths and skill paths exist.

## Scope (decision-complete)
- Files added/edited:
  - `plugins/codexclaw/scripts/build.mjs`
  - `plugins/codexclaw/components/*/dist/**`
  - `plugins/codexclaw/test/build.test.mjs`
- Components compiled:
  - `pabcd-state`
  - `config-guard`
  - `provider-bridge`
  - `subagent-config`
- Build behavior:
  - uses Node 24 `stripTypeScriptTypes`.
  - rewrites relative `.ts` import specifiers to `.js`.
  - writes compiled files under each component `dist/`.
  - validates hook command paths, MCP args, skills, and placeholder markers.
  - emits `build OK -- 12 files compiled, layout validated.`
- Must-NOT-Have:
  - No tsc/esbuild dependency.
  - No uncompiled `.ts` hook command in manifest JSON.
  - No placeholder markers in shipped runtime source or dist.

## IPABCD micro-cycle
- I (if interview-bearing): not interview-bearing.
- P: use zero-dependency Node 24 build and commit `dist/` for clone-only marketplace installs.
- A: audit corrected invalid strip mode assumptions and required honest stub handling.
- B: implemented `compileComponent`, specifier rewrite, and layout validation.
- C: build tests prove idempotent dist and no remaining `.ts` relative specifiers.
- D: done = `npm run build` compiles 12 files and validates layout.

## Acceptance (1-3 testable criteria)
- `npm run build` exits 0 and compiles 12 files.
- Two consecutive builds produce byte-identical dist.
- No compiled dist file has relative `.ts` import specifiers.

## QA channel (node:test path / CLI stdout / tmux / data dump)
- `plugins/codexclaw/test/build.test.mjs`: 6 tests total.
- Relevant tests: build idempotent, manifest paths exist, compiled dist rewrites specifiers,
  no placeholder markers.
- Final Phase 1: `npm run build` OK and root `npm test` 73/73.

## Commit unit (one atomic conventional commit)
Build compiler and validator landed in Pass 7 B commit a785b7a.

## Blocked-on (jun decision id, if any)
None.

## References (codex-rs paths, omo skills, ouroboros, source-of-record docs)
- `plugins/codexclaw/scripts/build.mjs`
- `plugins/codexclaw/test/build.test.mjs`
- `plugins/codexclaw/.codex-plugin/plugin.json`
- `devlog/_plan/260629_codexclaw_mvp/070.1_pass7_P_plan.md`
