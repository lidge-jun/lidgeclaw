# L9.4 (Decade 094) -- Mind Execution Boundary T7

Status: DONE
Cluster: 1 - IPABCD Interview completion - Phase: 1 - Shorthand: cxc
Source-of-record: 080.1, 080.2, 034.5, ouroboros 030/040

## Goal (one slice)
Lock Mind execution to top-level main-session dispatch and define nested-session degradation.

## Why now / dependencies
L9.1 and L9.2 define prompts and output. T7 is resolved so implementation can avoid hidden nested orchestration while still documenting last-resort inline behavior.

## Scope (decision-complete)
- Files to add/edit: `plugins/codexclaw/components/pabcd-state/src/hook.ts`, `plugins/codexclaw/components/pabcd-state/test/hook.test.ts`.
- T7 is resolved: five Mind contradiction subagents are spawned only from the top-level main session.
- Nested-session detection (EXACT, ordered): (1) PRIMARY = presence of subagent role metadata / injected subagent instructions in the hook payload context; (2) FALLBACK = absence of top-level `spawn_agent` capability. First positive signal wins. No other heuristic is used in MVP.
- If the main agent is itself already a subagent, it does not start a normal interview.
- Inline persona fallback is allowed only as a last resort, must be labeled degraded, and must not pretend to be independent subagent review.
- Preserve one correlation key per Mind result in normal and degraded modes.
- Routing remains adaptive: simple work may use one Mind, normal work two or three, large decisions more with recommended concurrent cap of three.
- Phase 2 per-Mind model settings stay deferred; MVP uses fixed prompts only.
- Must-NOT-Have: no hidden nested subagent spawn, no 9-way fan-out, no first-class Mind model config in MVP.

## IPABCD micro-cycle
- I: Main verifies top-level status before selecting and dispatching Minds.
- P: Encode T7 resolution and degraded fallback wording in directive text.
- A: Reviewer checks nested-session and spawn-unavailable cases for explicit reporting.
- B: Implement boundary text and dry-run diagnostics if available.
- C: Run hook tests proving top-level-only and degraded fallback language.
- D: Done = nested sessions skip normal interview or clearly label last-resort inline fallback.

## Acceptance (1-3 testable criteria)
1. Directive states top-level main session is the only normal Mind dispatcher.
2. Directive states nested sessions do not start normal interview.
3. Inline persona fallback is labeled last resort and degraded, not primary.

## QA channel (node:test path / CLI stdout / tmux / data dump)
- `node --test plugins/codexclaw/components/pabcd-state/test/hook.test.ts`
- Optional CLI stdout: dry-run prints selected Mind ids and execution mode.

## Commit unit (one atomic conventional commit)
`feat(interview): define top-level mind dispatch boundary`

## Blocked-on (jun decision id, if any)
None. T7 resolved to Option A: main-session-only dispatch; inline persona fallback is last resort only.

## References (codex-rs paths, omo skills, ouroboros, source-of-record docs)
- `devlog/_plan/260629_codexclaw_mvp/034.5_mind_model_injection.md`
- `devlog/_plan/mvp_res/000_research_src/ouroboros_interview/030_mind_to_subagent_map.md`
