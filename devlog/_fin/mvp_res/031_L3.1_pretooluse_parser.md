# L3.1 (Decade 031) -- PreToolUse Parser

Status: DONE
Cluster: 1 - Phase: 1 - Shorthand: cxc
Source-of-record: 023_goal_convention_port.md; 023.2_pass3_P_plan.md; STATUS.md

## Goal (one slice)
Parse Codex PreToolUse hook payloads into a safe typed shape for the goal-budget guard.

## Why now / dependencies
The goal guard can only deny budgeted `create_goal` calls if cxc reads the hook payload without
throwing and without assuming unknown fields. This sub-pass is independent from state mutation.

## Scope (decision-complete)
- Files added/edited:
  - `plugins/codexclaw/components/pabcd-state/src/goal-gate.ts`
  - `plugins/codexclaw/components/pabcd-state/src/cli.ts`
  - `plugins/codexclaw/components/pabcd-state/test/goal-gate.test.ts`
- Parser function:
  - `parsePreToolUse(raw)`
- Required input fields:
  - `hook_event_name = "PreToolUse"`
  - `session_id`
  - `cwd`
  - `tool_name`
- Preserved fields:
  - `tool_input`
  - `tool_use_id`
  - `turn_id`
  - `transcript_path`
  - `model`
  - `permission_mode`
- CLI dispatch:
  - `node dist/cli.js hook pre-tool-use`
  - invalid input exits 0 with no output.
- Must-NOT-Have:
  - No thrown error on empty, corrupt, array, or wrong-event input.
  - No reliance on goal server state.
  - No parsing of non-PreToolUse events as goal inputs.

## IPABCD micro-cycle
- I (if interview-bearing): not interview-bearing.
- P: add a stateless parser matching Codex snake_case fields.
- A: Pass 3 audit checked schema parity with codex-rs PreToolUse output and omo guard behavior.
- B: implemented `PreToolUsePayload`, record guards, string guards, and CLI branch.
- C: tests cover valid payload roundtrip, invalid input null, and parse-to-guard end-to-end.
- D: done = cxc can safely receive PreToolUse payloads without blocking Codex.

## Acceptance (1-3 testable criteria)
- Valid PreToolUse JSON returns all fields needed by the guard.
- Empty, corrupt, wrong-event, or missing required field input returns null.
- The CLI branch treats null parse as no-op output.

## QA channel (node:test path / CLI stdout / tmux / data dump)
- `plugins/codexclaw/components/pabcd-state/test/goal-gate.test.ts`: 9 tests total.
- Parser-specific tests: valid payload roundtrip, empty/corrupt/wrong-event null, parse then guard.
- Pass 3 component gate: pabcd-state 52/52; final root `npm test` 73/73.

## Commit unit (one atomic conventional commit)
PreToolUse parser and CLI branch landed in Pass 3 B commit 1331097.

## Blocked-on (jun decision id, if any)
None.

## References (codex-rs paths, omo skills, ouroboros, source-of-record docs)
- `plugins/codexclaw/components/pabcd-state/src/goal-gate.ts`
- `plugins/codexclaw/components/pabcd-state/src/cli.ts`
- `plugins/codexclaw/components/pabcd-state/test/goal-gate.test.ts`
- `devlog/_plan/260629_codexclaw_mvp/023_goal_convention_port.md`
- codex-rs PreToolUse schema and omo `ulw-loop/src/codex-hook.ts` guard parity.
