# Usability Patch Cycle — /help + thread-mode + GUI help/? + mode popup + favicon

Date: 2026-07-07. New HOTL goal + goalplan
`usability-patch-bridge-help-upgrade-mode-command`. Per explicit user
instruction, ALL subagents this cycle are anthropic/claude-opus-4-6 (no
gpt-5.5): auditor "Rawls", workers "Maxwell" (bridge) + "Halley" (GUI),
adversarial reviewer TBD at C.

## Design read (cxc-dev-uiux-design)
Ops dashboard surface: VARIANCE 2, MOTION 1 (single drawer slide), density D4,
radius <= 8px, Korean-first help copy, no emoji, stroke icons. Pattern source:
cli-jaw HelpDrawer/HelpTopicButton (interaction only, not visual style).

## Audit synthesis (Rawls round 1 FAIL -> spec.md rev 1 -> round 2 PASS)
Blockers: (1) plan said db v7 but v7 (topic rebuild) already landed -> v8
migration with CHECK; (2) thread_mode must land at FOUR sites (AgentPatch
db.ts:79-91, updateAgent allowlist :657-669, publicAgent agent-routes:23-41,
update handler :88-153); (3) GUI AgentInfo/AgentPatchBody missing threadMode —
and pre-existing drift: fullAccess/webhookUrl already returned by the server
but absent from GUI types (folded in).
Key advisories: DC /help iterates COMMANDS independently (discord-commands
:139-143) -> unified buildHelpEntries generator so help cannot drift; DC plain
mode = guard the SINGLE startThread site (replyChannelForMessage) + skip
admitThreadChannel, sweep skips plain naturally via the task-thread label
assumption (documented); TG plain mode = topicId null at both derivation sites,
reply routing untouched; GUI has NO page-header pattern -> new slim header row
on all 5 pages hosts the "?" button; favicon wiring verified against real vite
public/ + server.ts png serving.

## Diff-level scope
Bridge (Maxwell): db.ts v8 + 4 sites, discord-adapter plain guards,
telegram-adapter/webhook topicId guards, gateway-commands /mode +
buildHelpEntries, discord-commands help unification, tests.
GUI (Halley): api.ts types, NEW ui/help.tsx (HelpDrawer/HelpTopicButton/
HELP_CONTENT), page-header row on 5 pages, agent mode popup (Modal + [<] mode
[>] arrow switcher, ArrowLeft/Right keys, optimistic update + toast revert),
index.html favicon links + App.tsx logo mark. Favicon assets pre-generated by
main session (sips 32/64/180/512 from devlog/_fin/logo-selected/
codexclaw-final.png; 64px legibility verified visually).

## Completion (DONE, 2026-07-07)
- Gates: bridge 265/0 (`node --test 'test/*.test.ts'`), gui vite build OK
  (46 modules), repo `npm run build` 100 files, `npm run gate` OK,
  `cxc loop validate` OK (uc1-uc4 met with evidence).
- C-gate reviewer Zeno (claude-opus-4-6) PASS + 4 advisories; main session
  fixed the medium one (v8 migration now BEGIN/COMMIT/ROLLBACK guarded like
  v6/v7) and added the negative threadMode PATCH test (bogus->400,
  plain->200 round-trip). Remaining lows recorded as accepted: DC help embed
  has no runtime limit guard (currently ~940 chars / 2 fields vs 6000/25
  limits), TG getBinding resolves topicId for commands that ignore it.
- LIVE E2E incident: the running 7717 server (PID 41370, started 15:12) held
  OLD dist code in memory — the popup's optimistic "Plain" did not persist
  (API response lacked threadMode; DB still user_version 7). Fix: restarted
  `dist/cli.js serve` (cwd trend-viewer) detached via node_repl spawn; DB
  migrated to v8 on boot. Playwright then verified: help drawer on
  #/channels (Korean copy + /help /stop /approve refs), mode popup
  thread->plain->thread with API round-trip, favicon-32 served 200 image/png
  1871B, sidebar logo mark + page headers render. Agent left at
  thread_mode='thread' (default).
