# 100 — Dev Router Skill Prompt Gaps (CORE)

Gap class: PROMPT/SKILL · evidence: explorer #100

## Parity table

| reference (file:line) | codexclaw (file:line) | gap | jaw-style reinforcement |
| --- | --- | --- | --- |
| `plugins/omo/skills/programming/SKILL.md:14-30` hard-gates all code behind "LANGUAGE GATE" and says "DO NOT WRITE OR EDIT A SINGLE LINE" before loading the governing reference; `jawcode/.../system/system-prompt.md:121-127,171-173` also says active skill text must be followed exactly and "Search first, code second." | `plugins/codexclaw/skills/dev/SKILL.md:121-127,300-320` requires reading the matching `dev-*` router before writing, but the only explicit search obligation is for creating a new artifact, not for ordinary edits. | The router spine is still softer than the references on sequencing: it names routing and search duties, but it does not impose a universal pre-edit search/read gate for ordinary code changes. | Promote a first-step contract in `dev/SKILL.md`: classify -> load matching router(s) -> run repo search -> only then edit, using jaw's imperative order rather than today's split obligations. |
| `jawcode/.../system/system-prompt.md:110-119` ties C0-C5 classes to routing depth and verification scope; `cli-jaw/devlog/_fin/260608_dev_skills/87_work_class_tiering.md:15-22,49-64` sharpens the C2/C3/C4 boundary and says affected-suite depth scales with class. | `plugins/codexclaw/skills/dev/SKILL.md:17-35` classifies work, but C2/C3 language remains broad and the "add subagent audit when scope or risk warrants" phrasing is discretionary. | Classification exists, but the middle-band discipline is weaker than the jaw references: C2/C3 escalation language is less operational and easier to wave through. | Reword C2/C3/C4 rows in the jaw style: ordinary slice = compact plan + focused tests; cross-domain/public contract = explicit audit/plan trigger; high-risk = mandatory durable risk record. |
| `cli-jaw/devlog/_fin/260608_dev_skills/93_verification_intensity_matrix.md:15-22,58-70` gives per-class verification minimums and forbidden reporting language; `jawcode/.../system/system-prompt.md:148-159` forbids silent omission, fake completeness, and vague verification claims. | `plugins/codexclaw/skills/dev/SKILL.md:346-380` has a generic verification gate, a claim/requires/not-sufficient table (`:358-365`), and red-flag banned phrasing (`:376` already bans "should"/"probably"/"seems to"). Genuinely missing (A-gate correction): class-by-class minimums and a CRUD/UI-specific floor — the banned-phrasing block is NOT absent. | Verification-before-completion is present and already bans vague phrasing; the remaining gap is the per-class floor, not the banned words. | Add a compact per-class verification matrix (C2/C3/C4 minimums + CRUD/UI floor); reference the existing red-flag list at `:375-380` rather than re-adding banned phrases. |
| `plugins/omo/skills/remove-ai-slops/SKILL.md:31-34,46-52,79-91` defines slop as over-defensive code, broad catches, variant-chain logic, needless abstraction, dead code, and missing tests; `jawcode/.../system/system-prompt.md:149-154` separately bans stubs, placeholders, fake fallbacks, and TODO-only delivery. | `plugins/codexclaw/skills/dev/SKILL.md:399-423` covers only generic safety/quality signals and never gives the core router an anti-slop blacklist. | The core router lacks the reference projects' anti-slop doctrine: no universal ban on placeholder shipping, broad defensive clutter, speculative wrappers, or TODO-only "done" states. | Add a short router-level anti-slop section that names the non-negotiables and pushes detailed catalogs down to specialist skills, so the always-on spine rejects slop before surface routing even begins. |
| `plugins/omo/skills/programming/SKILL.md:185-235` makes the 250 pure-LOC ceiling a non-negotiable architectural defect; `plugins/omo/skills/remove-ai-slops/SKILL.md:82-100` repeats the same threshold and requires an executed split plan. | `plugins/codexclaw/skills/dev/SKILL.md:269-279` uses `>400 lines` as a DEFAULT hard limit that may be exceeded with a stated reason. | Codexclaw's modularity language is materially weaker: larger threshold, softer severity, and no "split during this task" mandate when the file is already oversized. | Tighten the router to a jaw/omo-style architectural rule: pure-LOC threshold, explicit exceptions, and mandatory split-now wording when new work lands in an already-oversized file. |
| `jawcode/.../system/eager-todo.md:1-12` requires a phased todo before substantive work; `jawcode/.../system/auto-continue.md:1` requires resuming the latest intent instead of stopping early. | `plugins/codexclaw/skills/dev/SKILL.md:99-112` explains goal-mode phase cycling, but ordinary dev work has no comparable todo/continuation discipline in the skill body. | The router skill does not embed the same execution persistence as jaw: no up-front phased checklist requirement for multi-step coding, and no explicit "keep going until the current intent is actually handled" wording. | Add a lightweight execution contract for C2+: initialize a short phased checklist, keep exactly one active step, and continue through verification unless blocked by a real approval/ambiguity seam. |

## Reinforcement shape

1. Add a top-of-skill execution order block with absolute verbs, not scattered prose: "classify first; read matching router(s); search first; then edit."
2. Replace the current C2/C3/C4 text with a tighter matrix that names the trigger, the required plan/audit depth, and the minimum verification floor per class.
3. Add a router-level anti-slop subsection: no placeholder delivery, no TODO-only completion, no speculative wrapper layers, no broad catch/defensive clutter without a named boundary reason.
4. Upgrade modularity from `>400 lines` DEFAULT guidance to a stricter architectural ceiling with explicit exception language and a "split in this task" obligation.
5. Add the per-class verification floor under the existing gate (C2/C3/C4 minimums + CRUD/UI floor). The banned-reporting block already exists at `dev/SKILL.md:375-380`; reference it rather than duplicating it. (Scoped down after A-gate found the banned-phrasing claim overstated.)
6. Add a lightweight persistence contract for multi-step work outside full PABCD: phased checklist first, one active step at a time, finish through verification unless a rule-classed ESCALATE seam stops execution.

Status: RESEARCH. No code changes, text-only proposals.
