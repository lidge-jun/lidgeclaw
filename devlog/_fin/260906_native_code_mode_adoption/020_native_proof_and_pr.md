# wp2 — Native forward testing and PR delivery

Dependency: wp1 policy/examples and deterministic remote tests passed.
C3 satisfy-spec. No merge/release/deployment or shared config changes.

## Exact work and files

NEW 021_verification.md in this unit records live catalog identity, actual call
sequences, native JS output/store recovery, rejected or unavailable-tool handling,
remote targeted/full suite output, review verdicts and limits. This is evidence,
not a new product runtime. No synthetic expected answer is called model behavior.

Run native functions.exec with actual safe tool discovery and two read-only
operations, same-session data projection and cache miss. Use distinct task keys;
retain yielded cell handles if returned, never substitute shell session IDs.
Evaluate at least two fresh Grok-4.6 forward tasks from common entrypoints, one
multi-read aggregation and one absent/failed capability scenario. Give only the
user task, entrypoint and raw fixtures, not intended decisions or prior verdicts.
Read actual tool calls/output, not final self-reported compliance alone. Do not
manufacture permission denials; controlled rejection is labeled synthetic.

MODIFY plugins/codexclaw/inventory.json and generated README.md/README.ko.md/README.zh.md/skills
README and docs-site count artifacts only as changed by the existing inventory
generator using a measured full-suite total. Inspect generator diff, do not guess
test counts. No version/cachebuster or dependency update in this PR.

On macmini, isolated exact-commit checkout: npm ci; targeted examples + packaging
+ skill catalog; npm test; node plugins/codexclaw/scripts/gate.mjs;
node plugins/codexclaw/scripts/inventory.mjs --check; npm run build and dist diff. The suite
owns executable example validation; semantic routing remains independent review.
No local product tests/build/typecheck. In-memory mutation tests run remotely.

Create one PR from codex/native-code-mode-routing to dev after fresh source review,
then read current head/check results. Fix any task-related failures and verify new
head. Record URL and source SHA in 021 and goalplan evidence. Keep old failure
artifacts, no blind reruns/waivers. No stack registration required for one PR.

At D, record source/policy/fixture/native evidence distinctions, unmet broader
research questions, and commit docs. Completed unit archives to devlog/_fin with
references updated if its path is linked. Do not mark the old research complete.

## Stop and reporting

DONE means common reachable policy + verified examples + bounded actual usage +
independent review + submitted PR. It does not mean forced use by every model,
proven speed/cost savings, all-platform native runtime certification or deployment.
