# wp5a — Version 0.2.24 surfaces and PR train (diff-level)

Depends on wp2, wp3, wp4 landing on dev. Class C4 (release surface).

## MODIFY version surfaces (0.2.23 → 0.2.24)

- package.json, cli/package.json, package-lock.json (root + workspace entries),
  plugins/codexclaw/gui/package.json and every
  plugins/codexclaw/components/*/package.json that carries 0.2.23.
- plugins/codexclaw/.codex-plugin/plugin.json: `0.2.24+codex.<UTC yyyymmddHHMMSS>`.
- plugins/codexclaw/inventory.json: version and measured counts (regenerate with the
  repository's inventory script if present; otherwise edit the version field and
  verify the badge test).
- CHANGELOG.md: rename the TOP `## [Unreleased]` (the one introduced by wp4 above
  0.2.23) to `## [0.2.24] - 2026-09-08`; the stale empty `## [Unreleased]` that sits
  below 0.2.19 (current line 57) is deleted in the same commit. Three bullets:
  reader-facing document structure (dev/reader-documents + routing), cxc-search deep
  research mode with Aside lane, PR #84 worktree source binding (credit
  thisisjun786).
- README.md / README.ko.md / README.zh.md: only if they print the version or the
  skill-family description changed in wp2/wp3 (checked at B; badges are measured
  by skill-catalog.test.mjs).

## PR train

1. Feature PRs to dev: `codex/narrative-documents` (wp2+wp3 skill changes and this
   unit's devlog) and `codex/pr84-integration` (wp4). Each waits for exact-head CI,
   packed-install and WSL; merge with merge commits.
2. Release prep commit on `codex/release-0.2.24` from the updated dev: version
   surfaces above; PR to dev; CI green; merge.
3. Promotion PR dev → main "release: promote 0.2.24 reader documents and deep
   research"; exact-head CI/packed/WSL on the PR head; merge.
4. Tag/Release: release.yml has two live triggers (workflow_dispatch with
   `prerelease` defaulting to true, and `tags: v*`). Use the tag path as 0.2.23 did
   (run 34177338827): create annotated tag v0.2.24 on the merged main SHA and push
   it. If dispatch is ever used instead, pass prerelease=false explicitly.
   Wait for success; download candidate-0.2.24.json, payload tar.gz, SHA256SUMS;
   verify checksums and that the payload matches `git archive` of the main SHA.

## Acceptance

- Release page v0.2.24 is Latest, non-draft; assets verified; delivery-state.json
  written under .codexclaw/evidence/narrative-release-0.2.24/ with run ids.

