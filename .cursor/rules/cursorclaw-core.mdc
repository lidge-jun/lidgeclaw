---
description: lidgeclaw / cursorclaw core — Cursor port of codexclaw discipline (hooks + skills + PABCD)
alwaysApply: true
---

# lidgeclaw → cursorclaw

This environment runs **cursorclaw**, the Cursor surface of the **lidgeclaw** umbrella (siblings: **zclaw** for ZCode, **claudeclaw** for Claude Code).

## What is armed

- **Hooks** (plugin + this repo `.cursor/hooks.json`): sessionStart, beforeSubmitPrompt, preToolUse, postToolUse, preCompact, stop, subagentStop — full Codex-era component fan-out via `scripts/cursor-bridge.mjs`.
- **Skills**: SoT `plugins/shared/skills/` (folder name == skill `name`). Host plugins symlink that tree. Do not also mirror into `~/.cursor/skills/` — Cursor would list each skill twice.
- **CLI**: `crc` / `cursorclaw` / `lidgeclaw` (`node bin/cursorclaw.mjs`).
- **State**: `.codexclaw/` (Cursor + ZCode + Claude share this project-local dir).

## Discipline

- Classify work C0–C5 via the `dev` skill before deep process.
- Full PABCD for C4; conditional for C3; never force on C0/C1.
- Prefer evidence over intent.

## Naming

Umbrella repo: **lidgeclaw**. Cursor plugin: **cursorclaw**. ZCode plugin: **zclaw**. Claude plugin: **claudeclaw**. Upstream Codex used `$crc-*` skill ids and `cxc` CLI; on Cursor those are `skills/<folder>` and `crc`.
